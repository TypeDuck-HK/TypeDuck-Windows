# TypeDuck Engine Runtime Contract

**Capture date:** 2026-06-23  
**Requirement traceability:** ENG-01, ENG-02, ENG-03, ENG-04, ENG-05  
**Decision traceability:** D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16

## Purpose

This contract records the Phase 2 developer runtime staging slice for TypeDuck Windows IME. It proves that the pinned TypeDuck-HK librime release, TypeDuck schema branch, lookup-filter provenance, dictionaries, OpenCC assets, and built schema outputs can be shaped into a Windows runtime tree for later adapter and engine probes.

Phase 2 uses the current `moqi-ime` runtime shape only as internal compatibility adapter evidence per D-01 and D-02. It is not product truth, does not approve legacy Moqi fallback behavior, and does not introduce final installer packaging, TSF registration, protocol, settings, or visible UI copy per D-14, D-15, and D-16.

Raw lookup-filter payload evidence and the ENG-05 handoff are recorded in `.planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md`.

## Locked Inputs

| Input | Value | Traceability |
|-------|-------|--------------|
| TypeDuck-HK librime release | `https://github.com/TypeDuck-HK/librime/releases/download/v1.1.3/rime-TypeDuck-v1.1.3-Windows-msvc-x86.7z` | D-05, D-06, ENG-01 |
| Release artifact handling | Treat the replacement `rime.dll` as available from the release; do not document how it was produced | D-07 |
| TypeDuck schema source | `https://github.com/TypeDuck-HK/schema`, branch `aap2-alpha` | D-08, ENG-03 |
| Lookup-filter source relation | `https://github.com/TypeDuck-HK/rime-dictionary-lookup-filter` | ENG-02 |
| Internal adapter boundary | Current backend-compatible `input_methods\rime\rime.dll` shape | D-01, D-02, D-03, D-04 |
| Banned product surfaces | `.planning/product/TYPEDUCK-BANNED-SURFACES.md` | D-14, D-15, D-16 |

## Staged Runtime Shape

The repeatable staging command is:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Stage-TypeDuckRuntime.ps1 -RepoRoot . -StageRoot .\runtime\typeduck-phase02 -ManifestPath .planning\product\engine-runtime-fixtures\phase-02\runtime-manifest.json
```

The staged tree is developer evidence only and is intentionally ignored by git:

| Path | Purpose |
|------|---------|
| `runtime\typeduck-phase02\input_methods\rime\rime.dll` | Compatibility-path TypeDuck-HK `rime.dll` for the current adapter shape. |
| `runtime\typeduck-phase02\input_methods\rime\*.schema.yaml` | TypeDuck schema files from `third_party\typeduck-schema` on `aap2-alpha`. |
| `runtime\typeduck-phase02\input_methods\rime\*.dict.yaml` and `essay.txt` | TypeDuck dictionary assets. |
| `runtime\typeduck-phase02\input_methods\rime\opencc\*` | TypeDuck OpenCC assets. |
| `runtime\typeduck-phase02\input_methods\rime\build\*` | Built schema artifacts generated by the pinned release `rime_deployer.exe`. |
| `runtime\typeduck-phase02\engine\*` | Extracted developer tooling and headers from the pinned release archive. |

## Evidence Manifest

Machine-readable evidence is recorded at:

`.planning/product/engine-runtime-fixtures/phase-02/runtime-manifest.json`

The manifest records:

- exact D-06 release URL, cached archive path, archive SHA-256, and extractor;
- `third_party\typeduck-schema` source URL, branch `aap2-alpha`, and resolved commit;
- staged `rime.dll` path, size, and SHA-256;
- lookup-filter source URL, commit from the release `version-info.txt`, and the staged runtime artifact hash;
- schema, dictionary, OpenCC, and built schema artifact paths with SHA-256 hashes;
- the deployer command, exit code, stdout, and stderr.

The contract validator is:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckRuntimeContract.ps1 -RepoRoot . -StageRoot .\runtime\typeduck-phase02 -ManifestPath .planning\product\engine-runtime-fixtures\phase-02\runtime-manifest.json
```

## Lookup-Filter Raw Payload Evidence

The raw lookup-filter contract is:

`.planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md`

It links the Plan 02-02 `typing-proof.json` candidate comments to:

- `.planning/product/engine-runtime-fixtures/phase-02/lookup-filter-raw-comments.ndjson`
- `.planning/product/engine-runtime-fixtures/phase-02/lookup-filter-csv-rows.csv`
- `.planning/product/engine-runtime-fixtures/phase-02/lookup-filter-validation.json`
- `scripts/Test-TypeDuckLookupPayload.ps1`

This evidence preserves D-10 column order and D-11/D-12 raw `\v`, `\f`, `\r`, quoted CSV, doubled-quote, and empty-field semantics without defining final protocol or UI mappings.

## Boundary Notes

- D-03 compatibility work is intentionally deferred to the adapter proof. If the current adapter's expected `rime_api` surface is incompatible with TypeDuck-HK librime, first determine the librime version currently used by the Moqi adapter, retrieve the upstream `rime_api` file for that version from `https://github.com/rime/librime`, diff it against `https://github.com/TypeDuck-HK/librime`, and inspect relevant upstream commits modifying the Rime API before patching.
- D-04 remains active: this contract does not move librime into the TSF DLL or rewrite the frontend around direct in-process calls.
- D-14 and D-15 remain active: Moqi, fcitx, cloud clipboard, WebDAV, AI, and legacy backend fallback surfaces are still banned by `.planning/product/TYPEDUCK-BANNED-SURFACES.md`.
- D-16 remains active: broad TypeDuck product naming, installer cleanup, final protocol fields, candidate UI, and settings UI belong to later phases.

## Verification Status

As of this capture, `scripts\Stage-TypeDuckRuntime.ps1` produces a manifest that passes `scripts\Test-TypeDuckRuntimeContract.ps1` for ENG-01, ENG-02, and ENG-03 staging evidence. `scripts\Invoke-TypeDuckEngineProof.ps1` records ENG-04 typing evidence in `typing-proof.json`, and `scripts\Test-TypeDuckLookupPayload.ps1` records ENG-05 lookup-filter evidence in `lookup-filter-validation.json`.
