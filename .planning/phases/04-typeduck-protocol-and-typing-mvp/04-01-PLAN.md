---
phase: 04-typeduck-protocol-and-typing-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "CMakeLists.txt"
  - "proto/moqi.proto"
  - "proto/ProtoFraming.h"
  - "Tests/TypeDuckProtocol/CMakeLists.txt"
  - "Tests/TypeDuckProtocol/ProtoFraming_test.cpp"
  - "scripts/Test-TypeDuckProtocolContract.ps1"
  - ".planning/product/protocol-fixtures/phase-04/protocol-contract.json"
autonomous: true
requirements_addressed:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - PROTO-06
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - PROTO-06
must_haves:
  truths:
    - "Developer can inspect explicit TypeDuck protocol semantics carried over the existing framed protobuf transport per D-01, D-02, and D-03."
    - "Protocol frames reject malformed or oversized payloads without unbounded buffering per D-13."
    - "Candidate messages can carry display text, selection/page metadata, and raw lookup-filter comment payloads without parsing them into Phase 5 UI fields per D-05, D-06, D-07, and D-08."
    - "Protocol fixtures cover normal Cantonese typing, raw lookup payload preservation, reverse-lookup marker preservation, malformed payloads, timeout, and restart cases per D-07 and D-16."
  artifacts:
    - path: "proto/moqi.proto"
      provides: "Compatibility protobuf schema extended with explicit TypeDuck protocol version, candidate/page, capability, settings snapshot/update, deploy/reconfigure, health, and error semantics."
    - path: "proto/ProtoFraming.h"
      provides: "Bounded frame encoder/parser helpers with deterministic oversized/malformed rejection."
    - path: "Tests/TypeDuckProtocol/ProtoFraming_test.cpp"
      provides: "Automated frame-bound and malformed-frame behavior tests."
    - path: "CMakeLists.txt"
      provides: "Root CMake registration for the TypeDuck protocol test subtree so `ProtoFraming_test` is discoverable."
    - path: "scripts/Test-TypeDuckProtocolContract.ps1"
      provides: "Static and fixture contract guard for TypeDuck protocol fields and raw comment preservation."
    - path: ".planning/product/protocol-fixtures/phase-04/protocol-contract.json"
      provides: "Golden protocol fixture matrix for Phase 4."
  key_links:
    - from: "proto/moqi.proto"
      to: "MoqiTextService/MoqiClient.cpp"
      via: "generated protobuf fields consumed by client response mapping in later Wave 3"
      pattern: "typeduck|TypeDuck|candidate_entries|candidate_cursor"
    - from: "proto/ProtoFraming.h"
      to: "MoqLauncher/PipeClient.cpp"
      via: "shared bounded FrameBuffer used by launcher client parsing in later Wave 2"
      pattern: "FrameBuffer"
    - from: "scripts/Test-TypeDuckProtocolContract.ps1"
      to: ".planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md"
      via: "validator asserts raw separator preservation instead of Phase 4 dictionary UI parsing"
      pattern: "LOOKUP-FILTER-RAW"
---

<objective>
Create the Phase 4 TypeDuck protocol and bounded frame foundation while preserving the existing TSF-to-launcher-to-backend transport per D-01.

Purpose: Give later waves explicit TypeDuck semantics to consume without treating Moqi protocol meanings as product truth per D-02, and prevent oversized or malformed frames from growing buffers without bounds per D-13.
Output: Extended protobuf schema, bounded framing helper, C++ frame tests, protocol contract guard, and golden fixture matrix for PROTO-01 through PROTO-06.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-CONTEXT.md
@.planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@CMakeLists.txt
@proto/moqi.proto
@proto/ProtoFraming.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED protocol and frame contract checks</name>
  <files>CMakeLists.txt, Tests/TypeDuckProtocol/CMakeLists.txt, Tests/TypeDuckProtocol/ProtoFraming_test.cpp, scripts/Test-TypeDuckProtocolContract.ps1, .planning/product/protocol-fixtures/phase-04/protocol-contract.json</files>
  <behavior>
    - Test 1: a frame larger than the configured Phase 4 maximum is rejected and the buffer enters a clean violation state without retaining the oversized body.
    - Test 2: a truncated frame remains pending only while the declared size is within the configured maximum.
    - Test 3: malformed protobuf payloads fail without producing a parsed request or response.
    - Test 4: fixture comments containing raw vertical-tab, form-feed, and carriage-return separators survive as byte-exact strings.
    - Test 5: the contract guard fails until `proto/moqi.proto` contains explicit TypeDuck protocol version, candidate/page, settings, capability, deploy/reconfigure, health, and error semantics.
  </behavior>
  <action>Create a product-level TypeDuck protocol test subtree and a PowerShell contract guard before changing production schema or framing. Register the C++ test target through root `CMakeLists.txt` by adding `add_subdirectory("${PROJECT_SOURCE_DIR}/Tests/TypeDuckProtocol")` or an equivalent root-discoverable registration so `cmake --build build-vs32 --config Debug --target ProtoFraming_test` can find the target. Follow the GoogleTest pattern documented in `.planning/codebase/TESTING.md`. The PowerShell guard must read `proto/moqi.proto`, `proto/ProtoFraming.h`, and `.planning/product/protocol-fixtures/phase-04/protocol-contract.json`; assert PROTO-01 through PROTO-06 coverage; and fail if raw lookup comments are parsed into native dictionary UI fields in Phase 4. Cite D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-13, D-14, and D-16 in the script failure messages or fixture metadata.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolContract.ps1 -RepoRoot . -Strict; if ($LASTEXITCODE -eq 0) { throw "Expected RED failure before protocol/framing implementation" }</automated>
  </verify>
  <done>The RED checks exist, fail for the current unbounded/Moqi-shaped contract, and describe the required TypeDuck semantics and raw-comment preservation without adding dictionary UI parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend protobuf semantics and bounded framing</name>
  <files>proto/moqi.proto, proto/ProtoFraming.h</files>
  <behavior>
    - Test 1: TypeDuck protocol version and compatibility-bridge fields are explicit without requiring a package rename that would break the current backend bridge.
    - Test 2: candidate responses can carry id, display text, input code, Jyutping, raw comment, translations/prompts when supplied, page info, and selected cursor metadata.
    - Test 3: settings snapshot/update, engine capability, deploy/reconfigure, engine health, and error messages are representable for later UI and recovery paths.
    - Test 4: `FrameBuffer` accepts an explicit maximum payload size and clears/rejects frames that exceed it.
  </behavior>
  <action>Update `proto/moqi.proto` so TypeDuck semantics are first-class while preserving the current framed protobuf transport and compatibility bridge per D-01 and D-02. Add named TypeDuck protocol/version, candidate, page, settings, capability, deploy/reconfigure, health, and error structures or fields that downstream code can consume. Keep `CandidateEntry.text/comment` usable as the Phase 4 raw-comment bridge per D-08, and add raw lookup comment preservation fields only if they do not require parsing lookup-filter CSV into UI fields per D-05 and D-06. Update `proto/ProtoFraming.h` with a bounded parser API, maximum payload constants for client and backend frames, violation state or error reporting, and helper behavior that rejects malformed/oversized frames deterministically per D-13.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolContract.ps1 -RepoRoot . -Strict; cmake -S . -B build-vs32 -G "Visual Studio 17 2022" -A Win32; cmake --build build-vs32 --config Debug --target ProtoFraming_test</automated>
  </verify>
  <done>`proto/moqi.proto` has explicit TypeDuck Phase 4 semantics, `ProtoFraming.h` enforces frame bounds, and both the protocol guard and frame test target pass.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate protocol outputs through the normal build path</name>
  <files>proto/moqi.proto, proto/ProtoFraming.h, .planning/product/protocol-fixtures/phase-04/protocol-contract.json</files>
  <action>Run the normal CMake/protobuf generation path and update the protocol fixture metadata with the generated-field names and maximum frame sizes. Do not hand-edit checked-in generated protobuf files unless the existing build uses them for this repository; if they are stale read-only artifacts, record that in the summary instead of editing generated output by hand. Ensure the fixture matrix includes common Cantonese input, raw lookup payload, optional reverse-lookup marker, malformed frame, timeout, and backend restart cases per PROTO-06 and D-07.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolContract.ps1 -RepoRoot . -Strict; cmake --build build-vs32 --config Debug --target ProtoFraming_test</automated>
  </verify>
  <done>Protocol contract metadata matches the schema and bounded framing implementation, with golden case coverage ready for launcher and TSF client waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TSF client to launcher pipe | Untrusted or broken local client data crosses into the launcher process. |
| Launcher to backend stdio | Backend output is untrusted framed protobuf data and may be malformed or oversized. |
| Backend candidate comments to frontend UI | Raw lookup-filter comment text can include control separators and CSV content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Denial of Service | `proto/ProtoFraming.h` | mitigate | Enforce maximum payload sizes before buffering a declared body and clear state on violations per D-13. |
| T-04-01-02 | Tampering | `proto/moqi.proto` compatibility bridge | mitigate | Make TypeDuck protocol/version/candidate/health semantics explicit per D-02 and D-03 so Moqi-only product meanings are not silently reused. |
| T-04-01-03 | Information Disclosure | raw lookup comments | mitigate | Preserve raw payload bytes for Phase 5 parsing evidence but do not log or expand them into additional UI fields in this phase per D-05 and D-06. |
| T-04-01-SC | Tampering | package installs | accept | No npm, pip, or cargo installs are planned. |
</threat_model>

<verification>
Run:
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolContract.ps1 -RepoRoot . -Strict
cmake --build build-vs32 --config Debug --target ProtoFraming_test
</verification>

<success_criteria>
PROTO-01 through PROTO-06 are planned through explicit TypeDuck protocol semantics, bounded frame parsing, raw lookup-comment survivability fixtures, health/error/settings/capability messages, and golden fixture coverage. D-01 through D-08 and D-13 through D-16 are respected without parsing dictionary UI fields in Phase 4.
</success_criteria>

<source_audit>
| Source | ID | Plan Coverage | Status |
|--------|----|---------------|--------|
| GOAL | Phase 4 bounded TypeDuck protocol | 04-01, 04-02, 04-03, 04-04 | COVERED |
| REQ | ENG-06 | 04-02, 04-03, 04-04 | COVERED |
| REQ | PROTO-01 | 04-01 | COVERED |
| REQ | PROTO-02 | 04-01, 04-02, 04-03 | COVERED |
| REQ | PROTO-03 | 04-01, 04-03, 04-04 | COVERED |
| REQ | PROTO-04 | 04-01, 04-03, 04-04 | COVERED with D-05/D-06/D-08 limiting Phase 4 to raw preservation and parser handoff, not native dictionary UI parsing |
| REQ | PROTO-05 | 04-01, 04-02 | COVERED |
| REQ | PROTO-06 | 04-01, 04-04 | COVERED |
| REQ | TYPE-01 | 04-03, 04-04 | COVERED |
| REQ | TYPE-02 | 04-03, 04-04 | COVERED |
| REQ | TYPE-03 | 04-03, 04-04 | COVERED |
| REQ | TYPE-04 | 04-03, 04-04 | COVERED |
| REQ | TYPE-05 | 04-02, 04-03, 04-04 | COVERED |
| CONTEXT | D-01 | 04-01, 04-02, 04-03 | COVERED |
| CONTEXT | D-02 | 04-01, 04-03 | COVERED |
| CONTEXT | D-03 | 04-01, 04-03 | COVERED |
| CONTEXT | D-04 | 04-01, 04-02, 04-03 | COVERED |
| CONTEXT | D-05 | 04-01, 04-03, 04-04 | COVERED |
| CONTEXT | D-06 | 04-01, 04-03, 04-04 | COVERED |
| CONTEXT | D-07 | 04-01, 04-04 | COVERED |
| CONTEXT | D-08 | 04-01, 04-03 | COVERED |
| CONTEXT | D-09 | 04-03, 04-04 | COVERED |
| CONTEXT | D-10 | 04-03, 04-04 | COVERED |
| CONTEXT | D-11 | 04-03, 04-04 | COVERED |
| CONTEXT | D-12 | 04-04 | COVERED |
| CONTEXT | D-13 | 04-01, 04-02, 04-03 | COVERED |
| CONTEXT | D-14 | 04-02, 04-03 | COVERED |
| CONTEXT | D-15 | 04-02, 04-03 | COVERED |
| CONTEXT | D-16 | 04-01, 04-02, 04-04 | COVERED |
| RESEARCH | skipped | User explicitly skipped research | EXCLUDED |
</source_audit>

<output>
Create `.planning/phases/04-typeduck-protocol-and-typing-mvp/04-01-SUMMARY.md` when done.
</output>
