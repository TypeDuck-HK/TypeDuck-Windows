---
phase: 06-privacy-security-and-scaffold-cleanup
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - MoqLauncher/PipeSecurity.cpp
  - MoqLauncher/PipeSecurity.h
  - MoqLauncher/PipeServer.cpp
  - MoqLauncher/PipeServer.h
  - MoqiTextService/MoqiClient.cpp
  - MoqiTextService/MoqiClient.h
  - Tests/TypeDuckProtocol/ProtoFraming_test.cpp
  - scripts/Test-TypeDuckLauncherProtocol.ps1
  - scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
autonomous: true
requirements:
  - SEC-04
  - SEC-01
must_haves:
  truths:
    - "TSF client and launcher use the TypeDuck-owned named-pipe namespace `\\\\.\\pipe\\<username>\\TypeDuckIME\\Launcher` per D-02, D-12, and D-13."
    - "Named-pipe ACLs avoid broad process-style access masks while preserving same-user/app-container usability per D-12 and D-13."
    - "Client-side launcher identity checks catch obvious same-user spoofing without rejecting past or future TypeDuck-compatible launchers per D-12 and D-13."
  artifacts:
    - path: "MoqLauncher/PipeSecurity.cpp"
      provides: "Explicit named-pipe DACL rights"
    - path: "MoqiTextService/MoqiClient.cpp"
      provides: "Compatibility-tolerant pipe server PID/path sanity check"
    - path: "scripts/Test-TypeDuckLauncherProtocol.ps1"
      provides: "IPC hardening static guard"
  key_links:
    - from: "MoqLauncher/PipeServer.cpp"
      to: "MoqiTextService/MoqiClient.cpp"
      via: "Both construct the same TypeDuck pipe namespace"
      pattern: "TypeDuckIME"
    - from: "MoqiTextService/MoqiClient.cpp"
      to: "MoqLauncher/PipeSecurity.cpp"
      via: "Client sanity check complements server-side ACL"
      pattern: "GetNamedPipeServerProcessId"
---

<objective>
Harden the TypeDuck launcher IPC boundary.

Purpose: Prevent obvious same-user spoofing and oversized-frame abuse without breaking upgrade/downgrade/future-channel compatibility.
Output: TypeDuck pipe namespace, narrower pipe ACL, tolerant launcher identity checks, and IPC hardening guards implementing D-12, D-13, D-16, D-23, D-24, D-25, and D-26.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-01-SUMMARY.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-02-SUMMARY.md
@.planning/codebase/CONCERNS.md
@proto/ProtoFraming.h
@MoqLauncher/PipeSecurity.cpp
@MoqLauncher/PipeServer.cpp
@MoqiTextService/MoqiClient.cpp
@scripts/Test-TypeDuckLauncherProtocol.ps1
</context>

## Artifacts this phase produces

- Renames pipe namespace to `\\.\pipe\<username>\TypeDuckIME\Launcher`.
- Renames launcher mutex/window class to TypeDuck-owned values if not already done by preceding plans.
- Replaces pipe DACL broad allow ACEs with explicit named-pipe read/write/connect-compatible rights.
- Removes `PROCESS_ALL_ACCESS_HEX` pipe access use.
- Implements `Client::isPipeCreatedByMoqiServer` as a compatibility-tolerant TypeDuck launcher sanity check.
- Adds guard assertions for pipe namespace, ACL rights, bounded frame behavior, and non-strict TypeDuck identity compatibility.

<tasks>

<task type="auto">
  <name>Task 1: Add IPC hardening guard assertions</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    scripts/Test-TypeDuckLauncherProtocol.ps1
    scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
    MoqLauncher/PipeSecurity.cpp
    MoqLauncher/PipeServer.cpp
    MoqiTextService/MoqiClient.cpp
    Tests/TypeDuckProtocol/ProtoFraming_test.cpp
  </read_first>
  <files>scripts/Test-TypeDuckLauncherProtocol.ps1, scripts/Test-TypeDuckPrivacySecurityCleanup.ps1, Tests/TypeDuckProtocol/ProtoFraming_test.cpp</files>
  <action>Extend static guards and existing protocol tests for the Phase 6 IPC contract. Assert the TypeDuck pipe namespace on both client and server, reject process-style broad access masks in pipe allow ACEs, require a real server PID/path sanity check around `GetNamedPipeServerProcessId`, and preserve Phase 4 1 MiB frame-cap tests. The guard must not require exact signer/version checks, because D-12 and D-13 require compatibility-tolerant identity checks.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLauncherProtocol.ps1 -RepoRoot . -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target ProtoFraming_test -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    Launcher protocol guard contains checks for `TypeDuckIME` pipe namespace, no process-style allow mask, and non-empty launcher server PID/path validation.
    `ProtoFraming_test` still covers oversize/truncated/malformed frame behavior.
    Before IPC implementation, guard fails on current legacy pipe/ACL/identity gaps.
  </acceptance_criteria>
  <done>IPC hardening has focused RED guard/test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Rename pipe namespace and narrow pipe ACL rights</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    MoqLauncher/PipeSecurity.cpp
    MoqLauncher/PipeSecurity.h
    MoqLauncher/PipeServer.cpp
    MoqLauncher/PipeServer.h
    MoqiTextService/MoqiClient.cpp
    MoqiTextService/MoqiClient.h
    scripts/Test-TypeDuckLauncherProtocol.ps1
  </read_first>
  <files>MoqLauncher/PipeSecurity.cpp, MoqLauncher/PipeSecurity.h, MoqLauncher/PipeServer.cpp, MoqLauncher/PipeServer.h, MoqiTextService/MoqiClient.cpp, MoqiTextService/MoqiClient.h, scripts/Test-TypeDuckLauncherProtocol.ps1</files>
  <action>Change both server and client pipe path construction to `\\.\pipe\<username>\TypeDuckIME\Launcher`. Change mutex/window-class constants to TypeDuck-owned names if still scaffold-named. Replace broad allow ACE rights in `PipeSecurityAttributes` with explicit pipe-compatible read/write/synchronize rights for LocalSystem, Administrators, the logon SID, and app-container compatibility where required. Keep the network deny ACE. Remove the process-all-access hex constant and comments that describe process rights for pipe objects.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLauncherProtocol.ps1 -RepoRoot . -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target MoqiLauncher MoqiTextService -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    Client and server pipe builders contain `TypeDuckIME` and no longer construct a product pipe namespace with the old scaffold path.
    `PipeSecurity.cpp` has no process-style all-access constant and no allow ACE granting broad generic-all rights for the logon SID.
    Debug launcher and text-service targets build.
  </acceptance_criteria>
  <done>Pipe namespace and ACLs are TypeDuck-owned and narrower.</done>
</task>

<task type="auto">
  <name>Task 3: Implement compatibility-tolerant launcher identity sanity checks</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    MoqiTextService/MoqiClient.cpp
    MoqiTextService/MoqiClient.h
    MoqiTextService/MoqiImeModule.cpp
    scripts/Test-TypeDuckLauncherProtocol.ps1
    scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
  </read_first>
  <files>MoqiTextService/MoqiClient.cpp, MoqiTextService/MoqiClient.h, scripts/Test-TypeDuckLauncherProtocol.ps1, scripts/Test-TypeDuckPrivacySecurityCleanup.ps1</files>
  <action>Replace the always-true server check with a TypeDuck launcher sanity check. Use `GetNamedPipeServerProcessId`, `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)`, and `QueryFullProcessImageNameW` when available. Reject only obvious mismatches, such as an accessible server executable whose basename is not TypeDuck launcher-shaped or whose resolved path is outside the configured TypeDuck program directory when that directory is known. If Windows denies inspection, keep compatibility by accepting the connection and logging bounded English trace detail, not a user-facing technical dialog. Do not add signer, exact version, or exact hash checks per D-12 and D-13.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLauncherProtocol.ps1 -RepoRoot . -Strict</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target MoqiTextService -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    `isPipeCreatedByMoqiServer` or its replacement no longer returns true unconditionally.
    The implementation checks server PID and process image path where available.
    The implementation does not require exact executable version, signature, or hash.
    Guard scripts and Debug text-service build pass.
  </acceptance_criteria>
  <done>Client identity sanity check catches obvious spoofing while preserving TypeDuck compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TSF client -> launcher pipe | In-process TSF DLL connects to a same-user named pipe that another process could spoof. |
| launcher pipe ACL -> local apps | Pipe DACL decides which local/app-container callers can connect. |
| client/backend frames -> memory | Framed protobuf payloads must remain bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Spoofing | named pipe server | mitigate | Add TypeDuck namespace and PID/path sanity check without strict version rejection. |
| T-06-05-02 | Elevation of Privilege | pipe DACL | mitigate | Replace process-style broad allow rights with explicit pipe rights. |
| T-06-05-03 | Denial of Service | frame parser | mitigate | Preserve and test Phase 4 bounded frame behavior. |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo package install occurs in this plan. |
</threat_model>

<verification>
Run launcher protocol, privacy cleanup, ProtoFraming, Debug launcher, and Debug text-service checks.
</verification>

<success_criteria>
IPC uses TypeDuck-owned namespace, bounded frames remain verified, pipe ACL rights are narrower, and spoofing checks reject obvious wrong-server cases without future-version lockout.
</success_criteria>

<output>
Create `.planning/phases/06-privacy-security-and-scaffold-cleanup/06-05-SUMMARY.md` when done.
</output>
