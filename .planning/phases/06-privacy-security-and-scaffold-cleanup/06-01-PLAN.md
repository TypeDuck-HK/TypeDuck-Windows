---
phase: 06-privacy-security-and-scaffold-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/MoqiTsf.iss
  - .gitmodules
  - installer/Inno-Setup-Chinese-Simplified-Translation
  - scripts/Test-TypeDuckInstallerSkeleton.ps1
autonomous: true
requirements:
  - IDEN-01
  - IDEN-03
  - LANG-01
  - LANG-02
  - SEC-05
  - VER-02
must_haves:
  truths:
    - "Installer wizard and Start Menu surfaces expose TypeDuck-owned bilingual Traditional Hong Kong Chinese and English copy per D-02, D-09, D-11, D-20, D-21, and D-22."
    - "The old installer running-process warning page is removed; known TypeDuck process cleanup is automated per D-04, D-17, and D-18."
    - "The Simplified Chinese Inno translation dependency is absent per D-10."
  artifacts:
    - path: "installer/MoqiTsf.iss"
      provides: "Product-owned installer language, first-page bitmap/text, Start Menu entries, and process cleanup contract"
    - path: ".gitmodules"
      provides: "Submodule list without the Simplified Chinese Inno translation dependency"
    - path: "scripts/Test-TypeDuckInstallerSkeleton.ps1"
      provides: "Installer static guard extended for Phase 6 installer copy/process cleanup"
  key_links:
    - from: "installer/MoqiTsf.iss"
      to: "TypeDuckSettings/resources/Installer.bmp"
      via: "Wizard bitmap configured from the Phase 5 frontend-owned resource"
      pattern: "Installer\\.bmp"
    - from: "scripts/Test-TypeDuckInstallerSkeleton.ps1"
      to: "installer/MoqiTsf.iss"
      via: "Strict static guard rejects legacy installer language/resource/process behavior"
      pattern: "Test-TypeDuckInstallerSkeleton"
---

<objective>
Clean the TypeDuck installer surface for Phase 6.

Purpose: Close visible installer localization, running-process, Start Menu, and Simplified-language scaffold leakage before release verification.
Output: Installer script and guard updates that implement D-02, D-03, D-04, D-09, D-10, D-11, D-17, D-18, D-20, D-21, D-22, D-23, D-24, D-25, and D-26.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
@.planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/phases/03-zh-hk-tsf-registration-and-installer-skeleton/03-02-SUMMARY.md
@.planning/phases/05-candidate-dictionary-settings-and-about-ui-parity/05-11-SUMMARY.md
@installer/MoqiTsf.iss
@.gitmodules
@scripts/Test-TypeDuckInstallerSkeleton.ps1
</context>

## Artifacts this phase produces

- Removes `.gitmodules` entry and working-tree vendor directory `installer/Inno-Setup-Chinese-Simplified-Translation` per D-10.
- Renames Start Menu folder contract to `TypeDuckIME` per D-21.
- Renames Start Menu shortcuts to `輸入法設定 IME Settings`, `關於 About TypeDuck…`, and `解除安裝 Uninstall` per D-22.
- Adds/uses installer first-page bitmap `Installer.bmp` with the Phase 5 About text block per D-20.
- Removes installer running-process/user-close surface and keeps TypeDuck-specific automated process cleanup per D-04, D-17, and D-18.
- Extends `scripts/Test-TypeDuckInstallerSkeleton.ps1` to guard the installer Phase 6 contracts per D-23 and D-24.

<tasks>

<task type="auto">
  <name>Task 1: Remove the legacy installer language dependency</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/product/TYPEDUCK-BANNED-SURFACES.md
    .gitmodules
    installer/MoqiTsf.iss
    scripts/Test-TypeDuckInstallerSkeleton.ps1
  </read_first>
  <files>.gitmodules, installer/Inno-Setup-Chinese-Simplified-Translation, scripts/Test-TypeDuckInstallerSkeleton.ps1</files>
  <action>Remove the Simplified Chinese Inno translation submodule entry and delete the vendored directory per D-10. Extend the installer skeleton guard so `-Strict` fails if the removed submodule path, `ChineseSimplified.isl`, or a `chinesesimplified` language entry is present under installer/product packaging surfaces, while preserving D-01 and D-24 allowances for non-visible scaffold identifiers.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckInstallerSkeleton.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <acceptance_criteria>
    .gitmodules has no `installer/Inno-Setup-Chinese-Simplified-Translation` section.
    `Test-Path installer/Inno-Setup-Chinese-Simplified-Translation` returns False.
    The strict installer guard prints a PASS line and exits 0.
  </acceptance_criteria>
  <done>Simplified installer translation dependency is gone and guarded per D-10 and D-23.</done>
</task>

<task type="auto">
  <name>Task 2: Replace installer page text and Start Menu entries</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/phases/05-candidate-dictionary-settings-and-about-ui-parity/05-CONTEXT.md
    installer/MoqiTsf.iss
    TypeDuckSettings/resources/Installer.bmp
    TypeDuckSettings/TypeDuckAboutDialog.cpp
  </read_first>
  <files>installer/MoqiTsf.iss, scripts/Test-TypeDuckInstallerSkeleton.ps1</files>
  <action>Update the installer first page to use `TypeDuckSettings\resources\Installer.bmp` and the exact Phase 5 About text block per D-20. Keep user-facing installer copy bilingual Traditional Hong Kong Chinese and English per D-09 and D-11. Change the Start Menu folder to the literal `TypeDuckIME` and change shortcut display names to `輸入法設定 IME Settings`, `關於 About TypeDuck…`, and `解除安裝 Uninstall` per D-21 and D-22. Keep source filename `installer/MoqiTsf.iss` unchanged per D-01.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckInstallerSkeleton.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <acceptance_criteria>
    `installer/MoqiTsf.iss` contains `WizardImageFile=..\TypeDuckSettings\resources\Installer.bmp` or equivalent Inno first-page bitmap wiring.
    `installer/MoqiTsf.iss` contains `{autoprograms}\TypeDuckIME\輸入法設定 IME Settings`, `{autoprograms}\TypeDuckIME\關於 About TypeDuck…`, and `{autoprograms}\TypeDuckIME\解除安裝 Uninstall`.
    The first-page text source includes the Phase 5 About intro/contact/credit bilingual text, not a new summary.
  </acceptance_criteria>
  <done>Installer first-page and Start Menu surfaces match D-20 through D-22.</done>
</task>

<task type="auto">
  <name>Task 3: Automate TypeDuck process cleanup without a user-running-process page</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    installer/MoqiTsf.iss
    SetupHelper/SetupHelper.cpp
    scripts/Test-TypeDuckInstallerSkeleton.ps1
  </read_first>
  <files>installer/MoqiTsf.iss, scripts/Test-TypeDuckInstallerSkeleton.ps1</files>
  <action>Eliminate the installer surface that tells users TypeDuck/backend processes are currently running per D-04. Keep `StopTypeDuckProcesses` installer-owned and TypeDuck-specific per D-17 and D-18, and expand only to known TypeDuck images needed for safe install/uninstall such as `TypeDuckLauncher.exe`, `TypeDuckSettings.exe`, `TypeDuckAbout.exe`, and the packaged runtime `server.exe` under the TypeDuck install tree. Do not add broad legacy process termination beyond the already documented allowlisted migration cleanup.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckInstallerSkeleton.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <acceptance_criteria>
    `installer/MoqiTsf.iss` no longer relies on Inno running-app pages for TypeDuck process cleanup.
    `StopTypeDuckProcesses` only calls TypeDuck-known process names and does not terminate wildcard or unrelated legacy images.
    Setup helper failures remain bilingual for user-visible messages, while technical details stay in logs unless restart or unrecoverable typing state must be communicated per D-16.
  </acceptance_criteria>
  <done>Installer cleanup is automated, narrow, and no longer asks users to close a TypeDuck/backend-running page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> installer UI | User-facing text must not misrepresent product identity or language. |
| installer -> OS process cleanup | Elevated installer can terminate processes and delete registry/files. |
| repo -> installer language resources | Vendored language resources can inject Simplified/off-scope user-visible chrome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | installer language resources | mitigate | Remove the Simplified translation submodule and add strict guard coverage per D-10 and D-23. |
| T-06-01-02 | Denial of Service | process cleanup | mitigate | Keep termination TypeDuck-specific and allowlisted per D-17 and D-18. |
| T-06-01-03 | Spoofing | Start Menu/app entries | mitigate | Use exact TypeDuckIME folder and entry names per D-21 and D-22. |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo package install occurs in this plan. |
</threat_model>

<verification>
Run the installer skeleton guard after every task. Do not run VM screenshot automation in Phase 6.
</verification>

<success_criteria>
Installer language dependencies, first-page content, Start Menu labels, and process cleanup behavior satisfy D-02 through D-04, D-09 through D-11, D-17, D-18, and D-20 through D-24.
</success_criteria>

<output>
Create `.planning/phases/06-privacy-security-and-scaffold-cleanup/06-01-SUMMARY.md` when done.
</output>
