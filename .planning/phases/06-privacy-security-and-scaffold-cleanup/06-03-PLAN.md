---
phase: 06-privacy-security-and-scaffold-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - backends.json
  - MoqLauncher/PipeServer.cpp
  - MoqLauncher/PipeServer.h
  - MoqiTextService/MoqiImeModule.cpp
  - MoqiTextService/MoqiImeModule.h
  - MoqiTextService/TypeDuckProfile.cpp
  - scripts/Test-TypeDuckLauncherProtocol.ps1
  - scripts/Test-TypeDuckSettingsAboutUi.ps1
  - scripts/Test-TypeDuckRuntimePackagePruning.ps1
autonomous: true
requirements:
  - IDEN-01
  - IDEN-03
  - SEC-03
  - SEC-05
must_haves:
  truths:
    - "Launcher and TSF module use a fixed TypeDuck runtime bridge rooted at `TypeDuckRuntime`, not a staged backend manifest, per D-02, D-05, D-12, D-13, and D-14."
    - "TSF Configure and tray Settings continue to launch only `TypeDuckSettings.exe` per D-14."
    - "Backend metadata cannot override first-party profile identity or generic config-tool launch behavior per D-03 and prior Phase 3/5 decisions."
  artifacts:
    - path: "MoqLauncher/PipeServer.cpp"
      provides: "In-code single TypeDuck runtime backend registration and TypeDuckRuntime path"
    - path: "MoqiTextService/MoqiImeModule.cpp"
      provides: "Fixed runtime-dir discovery without staged backend manifest dependency"
    - path: "backends.json"
      provides: "Deleted transitional manifest"
  key_links:
    - from: "MoqLauncher/PipeServer.cpp"
      to: "installer/stage/win32/TypeDuckIME/TypeDuckRuntime"
      via: "BackendServer command/workingDir points at the TypeDuck runtime package"
      pattern: "TypeDuckRuntime"
    - from: "MoqiTextService/MoqiImeModule.cpp"
      to: "MoqiTextService/TypeDuckProfile.cpp"
      via: "First-party profile and fixed runtime directory replace backend authority"
      pattern: "TypeDuckRuntime"
---

<objective>
Replace manifest-driven backend discovery with a fixed TypeDuck runtime bridge.

Purpose: Remove staged backend manifest trust and arbitrary backend configuration influence while keeping the established out-of-process TypeDuck runtime boundary.
Output: Frontend runtime discovery changes and guards implementing D-01, D-02, D-03, D-05, D-12, D-13, D-14, D-17, D-18, D-19, D-23, D-24, D-25, and D-26.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
@.planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-02-SUMMARY.md
@.planning/phases/05-candidate-dictionary-settings-and-about-ui-parity/05-07-SUMMARY.md
@MoqLauncher/PipeServer.cpp
@MoqLauncher/PipeServer.h
@MoqiTextService/MoqiImeModule.cpp
@MoqiTextService/TypeDuckProfile.cpp
@backends.json
@scripts/Test-TypeDuckLauncherProtocol.ps1
@scripts/Test-TypeDuckSettingsAboutUi.ps1
</context>

## Artifacts this phase produces

- Deletes top-level `backends.json` as a shipped/runtime authority.
- Introduces a fixed `TypeDuckRuntime` backend registration in launcher code using command `TypeDuckRuntime\server.exe`, working directory `TypeDuckRuntime`, and bridge name `typeduck-runtime-bridge`.
- Replaces TSF module backend directory discovery with a fixed `TypeDuckRuntime` directory list.
- Updates TypeDuck profile icon preference from runtime `moqi-ime` path to runtime `TypeDuckRuntime` path.
- Extends launcher/settings/runtime guards to reject manifest-driven config-tool and backend discovery.

<tasks>

<task type="auto">
  <name>Task 1: Add RED guards for manifest-free runtime discovery</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/phases/04-typeduck-protocol-and-typing-mvp/04-02-SUMMARY.md
    scripts/Test-TypeDuckLauncherProtocol.ps1
    scripts/Test-TypeDuckSettingsAboutUi.ps1
    scripts/Test-TypeDuckRuntimePackagePruning.ps1
    backends.json
  </read_first>
  <files>scripts/Test-TypeDuckLauncherProtocol.ps1, scripts/Test-TypeDuckSettingsAboutUi.ps1, scripts/Test-TypeDuckRuntimePackagePruning.ps1</files>
  <action>Extend existing guards so they expect a single in-code `typeduck-runtime-bridge` whose command and working directory use `TypeDuckRuntime`, and so they reject staged or source `backends.json` as a product runtime authority. Keep guard allowances for source identifiers per D-01 and D-24. Preserve Phase 5 checks that TSF Configure and launcher menu Settings use fixed `TypeDuckSettings.exe` and no backend-declared `configTool` fields per D-14.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLauncherProtocol.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <acceptance_criteria>
    The launcher protocol guard contains assertions for `typeduck-runtime-bridge`, `TypeDuckRuntime\server.exe`, and no product `backends.json` dependency.
    The settings/About guard still rejects `configTool`, `configToolParams`, and `configToolDir` in TSF/launcher settings entry points.
    Before production changes, the launcher protocol guard fails for the current manifest dependency.
  </acceptance_criteria>
  <done>Manifest-free runtime discovery has focused RED guard coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Inline the fixed TypeDuck runtime bridge</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    MoqLauncher/PipeServer.cpp
    MoqLauncher/PipeServer.h
    MoqLauncher/BackendServer.cpp
    backends.json
    scripts/Test-TypeDuckLauncherProtocol.ps1
  </read_first>
  <files>MoqLauncher/PipeServer.cpp, MoqLauncher/PipeServer.h, backends.json</files>
  <action>Remove top-level manifest loading from `PipeServer::initBackendServers` and construct exactly one `Json::Value` or equivalent `BackendServer` configuration in code for `typeduck-runtime-bridge`, command `TypeDuckRuntime\server.exe`, workingDir `TypeDuckRuntime`, and empty params. Keep optional `initInputMethods` compatibility scanning unable to override the first-party TypeDuck profile mapping. Delete `backends.json` from source control because Plan 06-02 removed it from staging. Do not rename internal C++ class/source identifiers per D-01.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLauncherProtocol.ps1 -RepoRoot . -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target MoqiLauncher -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    `PipeServer::initBackendServers` does not read `{install}\backends.json`.
    Source `backends.json` is deleted.
    The launcher guard passes and Debug `MoqiLauncher` target builds.
  </acceptance_criteria>
  <done>Launcher backend discovery uses fixed TypeDuckRuntime in-code configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Remove TSF module backend manifest dependency</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    MoqiTextService/MoqiImeModule.cpp
    MoqiTextService/MoqiImeModule.h
    MoqiTextService/TypeDuckProfile.cpp
    scripts/Test-TypeDuckTsfIdentity.ps1
    scripts/Test-TypeDuckSettingsAboutUi.ps1
    scripts/Test-TypeDuckIconPackaging.ps1
  </read_first>
  <files>MoqiTextService/MoqiImeModule.cpp, MoqiTextService/MoqiImeModule.h, MoqiTextService/TypeDuckProfile.cpp, scripts/Test-TypeDuckTsfIdentity.ps1, scripts/Test-TypeDuckIconPackaging.ps1, scripts/Test-TypeDuckSettingsAboutUi.ps1</files>
  <action>Replace `loadBackendDirs` file parsing with a fixed runtime directory list containing `TypeDuckRuntime`. Keep `onConfigure` routed to `TypeDuckSettings.exe` only per D-14. Update TypeDuck profile icon preference and guards to use `TypeDuckRuntime\icons\TypeDuck_Small.ico` if the runtime icon source remains packaged, with the existing DLL resource fallback preserved. Do not restore backend `ime.json` as product authority; optional compatibility scanning may only find supplemental metadata under the fixed TypeDuck runtime path.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckTsfIdentity.ps1 -RepoRoot . -Strict</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckSettingsAboutUi.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target MoqiTextService -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    `MoqiImeModule.cpp` contains the fixed `TypeDuckRuntime` directory and no longer opens `backends.json`.
    `onConfigure` still launches only `TypeDuckSettings.exe`.
    TSF identity and settings/About guards pass.
  </acceptance_criteria>
  <done>TSF module runtime discovery is fixed and backend config-tool launch remains removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install payload -> launcher config | Previously staged JSON could steer backend execution. |
| backend metadata -> TSF module | Runtime metadata can influence profile/config paths if trusted too broadly. |
| user -> Configure command | TSF Configure opens a local executable from the install directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | backend manifest | mitigate | Delete top-level manifest and use fixed in-code TypeDuckRuntime bridge. |
| T-06-03-02 | Elevation of Privilege | backend config tool launch | mitigate | Keep only fixed `TypeDuckSettings.exe` launch per D-14. |
| T-06-03-03 | Spoofing | runtime profile metadata | mitigate | Keep first-party TypeDuck profile mapping seeded before optional metadata scanning. |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo package install occurs in this plan. |
</threat_model>

<verification>
Run launcher protocol, TSF identity, settings/About, runtime pruning, and Debug launcher/text-service build checks.
</verification>

<success_criteria>
No shipped or source `backends.json` product authority remains, and fixed TypeDuck settings/runtime paths continue to build and pass guards.
</success_criteria>

<output>
Create `.planning/phases/06-privacy-security-and-scaffold-cleanup/06-03-SUMMARY.md` when done.
</output>
