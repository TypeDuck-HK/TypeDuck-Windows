---
phase: 06-privacy-security-and-scaffold-cleanup
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - MoqLauncher/Utils.cpp
  - MoqLauncher/Utils.h
  - MoqLauncher/PipeServer.cpp
  - MoqLauncher/PipeServer.h
  - MoqLauncher/BackendServer.cpp
  - MoqLauncher/BackendServer.h
  - MoqiTextService/MoqiClient.cpp
  - MoqiTextService/MoqiCandidateWindow.cpp
  - MoqiTextService/MoqiTextService.cpp
  - MoqiTextService/TsfLog.cpp
  - MoqiTextService/TsfLog.h
  - MoqiTextService/DllEntry.cpp
  - libIME2/src/DebugLogConfig.cpp
  - libIME2/src/TextService.cpp
  - proto/moqi.proto
  - scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
autonomous: true
requirements:
  - IDEN-01
  - IDEN-04
  - LANG-02
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Runtime logs and diagnostics use `%LOCALAPPDATA%\\TypeDuckIME` and `TypeDuckLauncher.json` per D-02, D-08, and Phase 6 Specific Ideas."
    - "Log/debug/printf output is English-only and avoids raw typed content by default per D-06 through D-08."
    - "Cloud clipboard/WebDAV/AI/fcitx callable paths are removed or inaccessible in the Windows frontend per D-03, D-15, and SEC-02."
    - "Legacy `%APPDATA%\\Moqi` files named in D-19 are treated only as exact cleanup targets, not migrated or reused as TypeDuck v1 state."
  artifacts:
    - path: "scripts/Test-TypeDuckPrivacySecurityCleanup.ps1"
      provides: "Strict diagnostics/privacy and off-scope feature guard"
    - path: "MoqLauncher/Utils.cpp"
      provides: "TypeDuck-owned local/roaming data path helpers"
    - path: "proto/moqi.proto"
      provides: "Protocol without v1 cloud clipboard upload surface"
  key_links:
    - from: "libIME2/src/DebugLogConfig.cpp"
      to: "MoqLauncher/PipeServer.cpp"
      via: "Both read/write `TypeDuckLauncher.json` under TypeDuck local app data"
      pattern: "TypeDuckLauncher\\.json"
    - from: "MoqLauncher/PipeServer.cpp"
      to: "MoqLauncher/BackendServer.cpp"
      via: "Cloud clipboard listener/upload path is removed on both sides"
      pattern: "cloud"
---

<objective>
Move diagnostics to TypeDuck-owned paths and remove off-scope cloud/AI/fcitx runtime paths from the Windows frontend.

Purpose: Meet Phase 6 privacy and scaffold-cleanup expectations before IPC hardening and aggregate guard closure.
Output: TypeDuck data/log path helpers, diagnostics privacy changes, protocol/frontend cloud removal, exact D-19 legacy roaming cleanup guard coverage, and a strict cleanup guard implementing D-02, D-03, D-06, D-07, D-08, D-15, D-16, D-19, D-23, D-24, D-25, and D-26.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
@.planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@MoqLauncher/Utils.cpp
@MoqLauncher/PipeServer.cpp
@MoqLauncher/BackendServer.cpp
@MoqiTextService/MoqiClient.cpp
@MoqiTextService/TsfLog.cpp
@libIME2/src/DebugLogConfig.cpp
@proto/moqi.proto
</context>

## Artifacts this phase produces

- Creates or extends `scripts/Test-TypeDuckPrivacySecurityCleanup.ps1`.
- Renames runtime local path contract to `%LOCALAPPDATA%\TypeDuckIME`.
- Renames runtime roaming path contract to `%APPDATA%\TypeDuckIME` only for narrow migration cleanup; no v1 cloud/account feature uses it.
- Guards that `%APPDATA%\Moqi\ai_config.json`, `%APPDATA%\Moqi\appearance_config.json`, `%APPDATA%\Moqi\moqi_auto_pair_symbols.json`, and `%APPDATA%\Moqi\moqi_custom_phrase.json` are exact D-19 cleanup targets only, with no whole-directory or wildcard roaming deletion.
- Renames launcher config to `TypeDuckLauncher.json`.
- Renames launcher log file to `TypeDuckLauncher.log`; TSF log files live under `%LOCALAPPDATA%\TypeDuckIME\Log`.
- Removes Windows frontend cloud clipboard listener/upload and protocol upload surface.
- Adds diagnostics guard coverage for English-only logs and absence of Simplified diagnostics text.

<tasks>

<task type="auto">
  <name>Task 1: Add TypeDuck path and diagnostics privacy guards</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
    .planning/product/TYPEDUCK-BANNED-SURFACES.md
    scripts/Test-TypeDuckIconPackaging.ps1
    MoqLauncher/Utils.cpp
    MoqLauncher/PipeServer.cpp
    MoqiTextService/MoqiClient.cpp
    libIME2/src/DebugLogConfig.cpp
  </read_first>
  <files>scripts/Test-TypeDuckPrivacySecurityCleanup.ps1</files>
  <action>Create a strict PowerShell guard for Phase 6 privacy/security cleanup. It must check TypeDuck-owned local data/log/config paths, English-only diagnostics, absence of Simplified Chinese log/debug/printf strings, absence of frontend cloud clipboard/WebDAV/AI/fcitx callable paths, absence of raw typed-content logging patterns, and D-19 exact legacy roaming cleanup coverage for `%APPDATA%\Moqi\ai_config.json`, `%APPDATA%\Moqi\appearance_config.json`, `%APPDATA%\Moqi\moqi_auto_pair_symbols.json`, and `%APPDATA%\Moqi\moqi_custom_phrase.json`. Guard logic must fail if cleanup code deletes `%APPDATA%\Moqi` wholesale, uses wildcard roaming deletion, migrates those files into `%APPDATA%\TypeDuckIME`, or treats them as active v1 configuration. It must explicitly allow non-visible scaffold source identifiers per D-01 and D-24. Add `-ExpectRed DiagnosticsPrivacy` for the current failing state before production edits.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict -ExpectRed DiagnosticsPrivacy</automated>
  </verify>
  <acceptance_criteria>
    Guard exists with `-RepoRoot`, `-BackendRoot`, `-Strict`, and expected-red support.
    Expected-red mode catches current legacy data/log path and cloud clipboard surfaces.
    Expected-red mode catches missing D-19 assertions for the four exact legacy roaming cleanup targets.
    Guard logic documents source-identifier allowance without allowing visible/user-facing banned surfaces.
  </acceptance_criteria>
  <done>Diagnostics/privacy cleanup has RED guard coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Rename data, config, and log paths to TypeDuck-owned locations</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    MoqLauncher/Utils.cpp
    MoqLauncher/Utils.h
    MoqLauncher/PipeServer.cpp
    MoqiTextService/MoqiClient.cpp
    MoqiTextService/MoqiCandidateWindow.cpp
    MoqiTextService/MoqiTextService.cpp
    MoqiTextService/TsfLog.cpp
    MoqiTextService/DllEntry.cpp
    libIME2/src/DebugLogConfig.cpp
    libIME2/src/TextService.cpp
  </read_first>
  <files>MoqLauncher/Utils.cpp, MoqLauncher/Utils.h, MoqLauncher/PipeServer.cpp, MoqiTextService/MoqiClient.cpp, MoqiTextService/MoqiCandidateWindow.cpp, MoqiTextService/MoqiTextService.cpp, MoqiTextService/TsfLog.cpp, MoqiTextService/TsfLog.h, MoqiTextService/DllEntry.cpp, libIME2/src/DebugLogConfig.cpp, libIME2/src/TextService.cpp, scripts/Test-TypeDuckPrivacySecurityCleanup.ps1</files>
  <action>Replace runtime local app data paths with `%LOCALAPPDATA%\TypeDuckIME`, replace the launcher config filename with `TypeDuckLauncher.json`, and use TypeDuck-owned log filenames. Rename or add utility helpers so roaming data, if still needed for migration cleanup, resolves to `%APPDATA%\TypeDuckIME` and is not used for cloud/account behavior. Ensure the four D-19 legacy roaming files remain exact cleanup targets only and are not read, migrated, or re-created as TypeDuck v1 state. Translate any non-English log/debug/printf lines to English per D-06 and D-07. Redact or remove diagnostics that routinely include raw composition, candidate text, clipboard text, or typed payloads unless they are behind explicit opt-in debug/trace and still bounded per D-08.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target MoqiLauncher MoqiTextService -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    Source paths use `TypeDuckIME`, `TypeDuckLauncher.json`, and TypeDuck log filenames for runtime diagnostics.
    Runtime source does not read, migrate, or re-create `%APPDATA%\Moqi\ai_config.json`, `%APPDATA%\Moqi\appearance_config.json`, `%APPDATA%\Moqi\moqi_auto_pair_symbols.json`, or `%APPDATA%\Moqi\moqi_custom_phrase.json`.
    The strict privacy cleanup guard reports PASS for path and English-only diagnostics checks.
    Debug/trace opt-in still works through the renamed launcher config path.
  </acceptance_criteria>
  <done>Runtime diagnostics are TypeDuck-owned and conservative by default.</done>
</task>

<task type="auto">
  <name>Task 3: Remove cloud clipboard and other off-scope frontend paths</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    MoqLauncher/PipeServer.cpp
    MoqLauncher/PipeServer.h
    MoqLauncher/BackendServer.cpp
    MoqLauncher/BackendServer.h
    proto/moqi.proto
    scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
  </read_first>
  <files>MoqLauncher/PipeServer.cpp, MoqLauncher/PipeServer.h, MoqLauncher/BackendServer.cpp, MoqLauncher/BackendServer.h, proto/moqi.proto, scripts/Test-TypeDuckPrivacySecurityCleanup.ps1</files>
  <action>Remove the Windows frontend cloud clipboard listener, debounce timer, clipboard read, upload scheduling, backend upload method, and protocol request field/method from TypeDuck v1 per D-03 and D-15. If protocol field numbers are removed, reserve them in `proto/moqi.proto` rather than reusing them. Keep tray notifications, settings deploy, and TypeDuck backend health behavior intact. Do not add any replacement sync/account UI.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
    <automated>cmake --build build-vs32 --config Debug --target MoqiLauncher ProtoFraming_test -- /m:1</automated>
  </verify>
  <acceptance_criteria>
    Launcher source no longer registers `AddClipboardFormatListener` for TypeDuck cloud upload behavior.
    Backend server source no longer exposes a cloud upload method.
    `proto/moqi.proto` no longer exposes a v1 cloud clipboard request surface; removed field/method numbers are reserved where needed.
    Strict privacy cleanup guard reports PASS for cloud/WebDAV/AI/fcitx frontend surface checks.
  </acceptance_criteria>
  <done>Off-scope cloud clipboard/WebDAV/AI/fcitx frontend surfaces are gone or inaccessible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TSF host -> local logs | Host-process diagnostics can expose typed content or app paths. |
| launcher -> backend | Launcher can forward sensitive clipboard or typed payloads. |
| local storage -> user privacy | Config/log paths reveal product identity and may retain legacy cloud data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | logs/debug files | mitigate | Move to TypeDuck paths and redact/remove raw typed content by default per D-08. |
| T-06-04-02 | Information Disclosure | cloud clipboard | mitigate | Remove frontend listener/upload/protocol surface per D-15. |
| T-06-04-03 | Repudiation | diagnostic text | mitigate | Keep English-only bounded technical logs, with user-facing failure details minimized per D-06 and D-16. |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo package install occurs in this plan. |
</threat_model>

<verification>
Run the privacy cleanup guard and focused Debug launcher/text-service/protocol builds. Do not run VM screenshot automation.
</verification>

<success_criteria>
Diagnostics use TypeDuck-owned locations, avoid raw content by default, and no Windows frontend cloud/AI/fcitx/WebDAV v1 path remains callable.
</success_criteria>

<output>
Create `.planning/phases/06-privacy-security-and-scaffold-cleanup/06-04-SUMMARY.md` when done.
</output>
