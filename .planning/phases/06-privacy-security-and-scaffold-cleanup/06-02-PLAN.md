---
phase: 06-privacy-security-and-scaffold-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install.ps1
  - scripts/_all_in_package.ps1
  - installer/build-installer.ps1
  - D:/VSProjects/moqi-ime/scripts/build.ps1
  - scripts/Test-TypeDuckRuntimePackagePruning.ps1
  - scripts/Test-TypeDuckIconPackaging.ps1
  - scripts/Test-TypeDuckAppearanceTheme.ps1
autonomous: true
requirements:
  - IDEN-01
  - IDEN-04
  - SEC-02
  - VER-02
must_haves:
  truths:
    - "The shipped TypeDuck runtime folder is TypeDuck-owned, not scaffold-named, per D-02 and D-05."
    - "Off-scope runtime files for cloud clipboard, WebDAV, AI, Android, templates, tests, duplicate theme copies, and generic profile metadata are absent from the staged TypeDuck package per D-03, D-05, and D-15."
    - "Runtime pruning is enforced by automated scripts per D-23 through D-25."
  artifacts:
    - path: "scripts/Test-TypeDuckRuntimePackagePruning.ps1"
      provides: "Strict guard for banned runtime package paths/files"
    - path: "scripts/install.ps1"
      provides: "TypeDuckRuntime staging and app-root package filtering"
    - path: "D:/VSProjects/moqi-ime/scripts/build.ps1"
      provides: "Sibling runtime package pruning at source build time"
  key_links:
    - from: "D:/VSProjects/moqi-ime/scripts/build.ps1"
      to: "scripts/install.ps1"
      via: "sibling runtime build output consumed by installer staging"
      pattern: "TypeDuckRuntime"
    - from: "scripts/Test-TypeDuckRuntimePackagePruning.ps1"
      to: "installer/stage/win32/TypeDuckIME"
      via: "optional staged package scan after install.ps1"
      pattern: "TypeDuckRuntime"
---

<objective>
Make the shipped runtime package TypeDuck-owned and remove off-scope scaffold payload.

Purpose: Stop TypeDuck v1 from shipping callable or visible cloud/AI/fcitx/scaffold runtime paths before frontend discovery is rewired in Plan 06-03.
Output: Runtime/staging scripts and package guards implementing D-02, D-03, D-05, D-15, D-23, D-24, D-25, and D-26.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
@.planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/phases/05-candidate-dictionary-settings-and-about-ui-parity/05-12-SUMMARY.md
@scripts/install.ps1
@scripts/_all_in_package.ps1
@installer/build-installer.ps1
@scripts/Test-TypeDuckIconPackaging.ps1
@D:/VSProjects/moqi-ime/scripts/build.ps1
</context>

## Artifacts this phase produces

- Creates `scripts/Test-TypeDuckRuntimePackagePruning.ps1`.
- Renames the installed runtime folder contract from `moqi-ime` to `TypeDuckRuntime`.
- Updates package/stage scripts to exclude `android`, `cloudclipboard`, `templates`, `test`, `ai_config.json`, runtime `ime.json`, duplicate icon folders, and `input_methods/rime/data/appearance_themes.json`.
- Updates sibling backend package output default from `scripts/build/moqi-ime` to `scripts/build/TypeDuckRuntime` while keeping input parameter compatibility where needed.
- Removes generated `backends.moqi-ime.json` emission from sibling packaging because Plan 06-03 inlines the runtime bridge.

<tasks>

<task type="auto">
  <name>Task 1: Add a runtime package pruning guard</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
    scripts/Test-TypeDuckIconPackaging.ps1
    scripts/install.ps1
    D:/VSProjects/moqi-ime/scripts/build.ps1
  </read_first>
  <files>scripts/Test-TypeDuckRuntimePackagePruning.ps1, scripts/Test-TypeDuckIconPackaging.ps1, scripts/Test-TypeDuckAppearanceTheme.ps1</files>
  <action>Create a focused PowerShell guard using the Phase 5 guard shape. It must accept `-RepoRoot`, `-BackendRoot`, `-Strict`, and an optional `-ExpectRed RejectedRuntimePackage` mode. The guard must scan source packaging scripts and, when present, `installer/stage/win32/TypeDuckIME/TypeDuckRuntime` plus the sibling package output. It must fail on shipped runtime paths or files listed in Phase 6 Specific Ideas, while allowing internal source names per D-01 and D-24. Update existing icon/theme guards so their staged runtime paths use `TypeDuckRuntime` and canonical `input_methods/rime/appearance_themes.json` only.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckRuntimePackagePruning.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict -ExpectRed RejectedRuntimePackage</automated>
  </verify>
  <acceptance_criteria>
    The new guard exists and contains path assertions for `TypeDuckRuntime`, `input_methods\rime\appearance_themes.json`, and the Phase 6 banned runtime files/directories.
    Expected-red mode fails only because current package/staging behavior has not yet been fixed.
    Existing Phase 5 icon/theme guards no longer hard-code staged `moqi-ime` paths as the final installed runtime folder.
  </acceptance_criteria>
  <done>Runtime package pruning has a failing strict guard before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Prune the sibling TypeDuck runtime at package build time</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    D:/VSProjects/moqi-ime/scripts/build.ps1
    D:/VSProjects/moqi-ime/input_methods/rime/appearance_themes.json
    scripts/Test-TypeDuckRuntimePackagePruning.ps1
  </read_first>
  <files>D:/VSProjects/moqi-ime/scripts/build.ps1</files>
  <action>Update the sibling runtime build so the default package output is `scripts/build/TypeDuckRuntime`. Package only the TypeDuck Rime runtime pieces needed by Windows v1 and delete or exclude off-scope files/directories named in Phase 6 Specific Ideas per D-05 and D-15. Stop copying `appearance_themes.json` into `input_methods/rime/data`; keep the canonical root file. Stop generating the old backend snippet because Plan 06-03 will inline the single bridge. Change user-facing and build-output text to TypeDuck wording; build logs may be English-only per D-06.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File D:\VSProjects\moqi-ime\scripts\build.ps1 -RepoRoot D:\VSProjects\moqi-ime -RimeDataSource I:\GitHub\TypeDuck-Web\schema</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckRuntimePackagePruning.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
  </verify>
  <acceptance_criteria>
    `D:\VSProjects\moqi-ime\scripts\build\TypeDuckRuntime\server.exe` exists after the build.
    `D:\VSProjects\moqi-ime\scripts\build\TypeDuckRuntime\input_methods\rime\appearance_themes.json` exists.
    The strict pruning guard reports PASS and no banned runtime package paths from D-05/D-15 exist in the package output.
  </acceptance_criteria>
  <done>Sibling package output is TypeDuck-owned and pruned before Windows staging consumes it.</done>
</task>

<task type="auto">
  <name>Task 3: Stage the pruned runtime under the TypeDuck installed tree</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    scripts/install.ps1
    scripts/_all_in_package.ps1
    installer/build-installer.ps1
    scripts/Test-TypeDuckRuntimePackagePruning.ps1
  </read_first>
  <files>scripts/install.ps1, scripts/_all_in_package.ps1, installer/build-installer.ps1, scripts/Test-TypeDuckRuntimePackagePruning.ps1</files>
  <action>Rename staging variables and user-visible script text from the transitional runtime folder to `TypeDuckRuntime`, while preserving parameter compatibility only where callers still pass the sibling checkout path. Remove `backends.json` as a required staged file in the installer builder because Plan 06-03 owns in-code runtime registration. Stage the pruned sibling package into `%PROGRAMFILES(x86)%\TypeDuckIME\TypeDuckRuntime` and update icon stamping/package guards to inspect `TypeDuckRuntime\server.exe`.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\install.ps1 -RepoRoot . -MoqiImeSource D:\VSProjects\moqi-ime\scripts\build\TypeDuckRuntime</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckRuntimePackagePruning.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
  </verify>
  <acceptance_criteria>
    `installer/stage/win32/TypeDuckIME/TypeDuckRuntime/server.exe` exists after staging.
    `installer/stage/win32/TypeDuckIME/backends.json` does not exist.
    `installer/build-installer.ps1` no longer requires staged `backends.json`.
    The strict pruning guard reports PASS against both source package and staged payload.
  </acceptance_criteria>
  <done>Windows installer staging consumes the pruned TypeDuck runtime and no longer stages `backends.json`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling runtime -> installer payload | External sibling package files become shipped TypeDuck files. |
| package scripts -> installed tree | Build/stage scripts choose runtime paths, files, and executable resources. |
| guard scripts -> release readiness | Automated checks determine whether off-scope runtime content can ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | cloud/AI runtime configs | mitigate | Exclude cloud/AI files and directories from shipped package and guard absence per D-05 and D-15. |
| T-06-02-02 | Tampering | backend manifest staging | mitigate | Remove staged `backends.json`; Plan 06-03 replaces it with a fixed in-code TypeDuck runtime bridge. |
| T-06-02-03 | Spoofing | installed runtime folder identity | mitigate | Use `TypeDuckRuntime` folder instead of scaffold runtime folder per D-02. |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo package install occurs in this plan. |
</threat_model>

<verification>
Run sibling build, Windows staging, icon/theme guards updated for `TypeDuckRuntime`, and the new pruning guard. Do not run VM screenshot automation.
</verification>

<success_criteria>
The source-built and staged TypeDuck runtime are pruned, TypeDuck-owned, and guard-enforced without broad source identifier churn.
</success_criteria>

<output>
Create `.planning/phases/06-privacy-security-and-scaffold-cleanup/06-02-SUMMARY.md` when done.
</output>
