---
phase: 06-privacy-security-and-scaffold-cleanup
plan: 06
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
  - scripts/Test-TypeDuckRuntimePackagePruning.ps1
  - scripts/Test-TypeDuckInstallerSkeleton.ps1
  - scripts/Test-TypeDuckLauncherProtocol.ps1
  - scripts/Test-TypeDuckSettingsAboutUi.ps1
  - scripts/Test-TypeDuckIconPackaging.ps1
  - scripts/_all_in_package.ps1
  - .github/workflows/release.yml
  - .github/workflows/nightly.yml
  - installer/README.txt
  - .planning/product/privacy-security/phase-06-guard-results.json
autonomous: true
requirements:
  - IDEN-01
  - IDEN-03
  - IDEN-04
  - LANG-01
  - LANG-02
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - VER-02
must_haves:
  truths:
    - "Developer can run one strict Phase 6 guard suite that fails on visible Moqi/fcitx/WebDAV/cloud clipboard/AI/Simplified-only surfaces per D-23 and VER-02."
    - "The guard suite allows non-visible scaffold source identifiers per D-01 and D-24."
    - "The guard suite proves D-19 cleanup coverage for TypeDuck-owned DLL registrations, startup entries, scheduled tasks, install files, and exact allowlisted scaffold residue without broad user-state purges."
    - "CI/release artifact names and script invocation use TypeDuck-owned naming and `pwsh` per D-02 and project conventions."
  artifacts:
    - path: "scripts/Test-TypeDuckPrivacySecurityCleanup.ps1"
      provides: "Aggregate Phase 6 banned-surface, diagnostics, IPC, installer, and runtime guard"
    - path: ".planning/product/privacy-security/phase-06-guard-results.json"
      provides: "Guard result evidence recorded by execution"
    - path: ".github/workflows/release.yml"
      provides: "TypeDuck release artifact naming and pwsh script invocation"
  key_links:
    - from: "scripts/Test-TypeDuckPrivacySecurityCleanup.ps1"
      to: "scripts/Test-TypeDuckRuntimePackagePruning.ps1"
      via: "Aggregate guard invokes runtime package pruning"
      pattern: "Test-TypeDuckRuntimePackagePruning"
    - from: ".github/workflows/release.yml"
      to: "scripts/_all_in_package.ps1"
      via: "CI invokes package script and uploads TypeDuck-named installer"
      pattern: "typeduck-windows-ime-setup\\.exe"
---

<objective>
Close Phase 6 with aggregate guard coverage and TypeDuck release artifact naming.

Purpose: Make VER-02 executable and prevent visible scaffold/privacy regressions before Phase 7 release verification.
Output: Strict guard suite, guard result evidence, CI/release artifact naming updates, and docs alignment implementing D-02, D-03, D-06 through D-26.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-PATTERNS.md
@.planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/codebase/TESTING.md
@scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
@scripts/Test-TypeDuckRuntimePackagePruning.ps1
@scripts/Test-TypeDuckInstallerSkeleton.ps1
@scripts/Test-TypeDuckLauncherProtocol.ps1
@scripts/_all_in_package.ps1
@.github/workflows/release.yml
@.github/workflows/nightly.yml
</context>

## Artifacts this phase produces

- Finalizes `scripts/Test-TypeDuckPrivacySecurityCleanup.ps1` as the aggregate Phase 6 guard.
- Finalizes `scripts/Test-TypeDuckRuntimePackagePruning.ps1` as the runtime package guard.
- Extends aggregate guard coverage for D-19 cleanup: TypeDuck-owned TSF DLL registrations, startup entries, scheduled tasks, install files, and exact legacy roaming targets `%APPDATA%\Moqi\ai_config.json`, `%APPDATA%\Moqi\appearance_config.json`, `%APPDATA%\Moqi\moqi_auto_pair_symbols.json`, and `%APPDATA%\Moqi\moqi_custom_phrase.json`.
- Produces `.planning/product/privacy-security/phase-06-guard-results.json` during execution.
- Renames CI workflow artifact names to `typeduck-windows-ime-release-*` and `typeduck-windows-ime-nightly-*`.
- Renames CI upload paths/assets to `installer/dist/typeduck-windows-ime-setup.exe`.
- Replaces workflow `.ps1` invocations with `pwsh -NoProfile -ExecutionPolicy Bypass -File`.
- Updates `installer/README.txt` to TypeDuck staging/runtime names.

<tasks>

<task type="auto">
  <name>Task 1: Finalize aggregate banned-surface guard</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/product/TYPEDUCK-BANNED-SURFACES.md
    scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
    scripts/Test-TypeDuckRuntimePackagePruning.ps1
    scripts/Test-TypeDuckInstallerSkeleton.ps1
    scripts/Test-TypeDuckLauncherProtocol.ps1
    scripts/Test-TypeDuckSettingsAboutUi.ps1
    scripts/Test-TypeDuckIconPackaging.ps1
  </read_first>
  <files>scripts/Test-TypeDuckPrivacySecurityCleanup.ps1, scripts/Test-TypeDuckRuntimePackagePruning.ps1, scripts/Test-TypeDuckInstallerSkeleton.ps1, scripts/Test-TypeDuckLauncherProtocol.ps1, scripts/Test-TypeDuckSettingsAboutUi.ps1, scripts/Test-TypeDuckIconPackaging.ps1</files>
  <action>Make `Test-TypeDuckPrivacySecurityCleanup.ps1 -Strict` the aggregate Phase 6 guard. It must invoke or duplicate the focused checks for installer localization/process cleanup, runtime package pruning, fixed settings launch, icon/package path updates, TypeDuck data/log paths, English-only diagnostics, cloud/AI/fcitx/WebDAV absence, pipe namespace/ACL/identity hardening, and source/user-facing scan boundaries. Add explicit D-19 guard assertions that installer/setup-helper cleanup covers TypeDuck-owned Win32 and x64 TSF DLL registration cleanup, TypeDuck startup-entry cleanup, TypeDuck scheduled-task cleanup, TypeDuck install-file cleanup, and exact allowlisted scaffold residue cleanup for `%APPDATA%\Moqi\ai_config.json`, `%APPDATA%\Moqi\appearance_config.json`, `%APPDATA%\Moqi\moqi_auto_pair_symbols.json`, and `%APPDATA%\Moqi\moqi_custom_phrase.json`. The guard must fail if cleanup uses whole-directory `%APPDATA%\Moqi` deletion, wildcard roaming deletion, or broad legacy user-state purging. It must not fail merely because internal source paths, target names, namespaces, class names, or script parameter names still contain scaffold identifiers per D-01 and D-24.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
  </verify>
  <acceptance_criteria>
    Aggregate guard exits 0 and prints a PASS line.
    Guard source includes explicit scoped allowlist behavior for internal identifiers.
    Guard source includes checks for all Phase 6 banned surface categories from D-23.
    Guard source includes D-19 checks for TypeDuck-owned DLL registration cleanup, startup cleanup, scheduled-task cleanup, install-file cleanup, and the four exact `%APPDATA%\Moqi\*.json` cleanup targets.
    Guard source rejects broad `%APPDATA%\Moqi` directory deletion and wildcard roaming cleanup.
  </acceptance_criteria>
  <done>VER-02 has one strict aggregate guard path.</done>
</task>

<task type="auto">
  <name>Task 2: Rename CI artifact surfaces and script invocations</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
    .github/workflows/release.yml
    .github/workflows/nightly.yml
    scripts/_all_in_package.ps1
    installer/README.txt
  </read_first>
  <files>.github/workflows/release.yml, .github/workflows/nightly.yml, scripts/_all_in_package.ps1, installer/README.txt, scripts/Test-TypeDuckPrivacySecurityCleanup.ps1</files>
  <action>Update GitHub Actions release/nightly workflow surfaces to TypeDuck artifact names per D-02 and IDEN-01. Use `typeduck-windows-ime-setup.exe` for upload paths, `typeduck-windows-ime-release-*` and `typeduck-windows-ime-nightly-*` for workflow artifact names, and remove clean-runner icon seeding that copies legacy icon files into old staged paths. Invoke project PowerShell scripts with `pwsh -NoProfile -ExecutionPolicy Bypass -File` per project conventions. Update installer README staging examples to `TypeDuckIME` and `TypeDuckRuntime`.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
  </verify>
  <acceptance_criteria>
    Release workflow uploads `installer/dist/typeduck-windows-ime-setup.exe`.
    Nightly workflow uploads `installer/dist/typeduck-windows-ime-setup.exe`.
    Workflow artifact names are TypeDuck-owned.
    Workflows do not invoke project `.ps1` files through `powershell.exe`.
    `installer/README.txt` documents `TypeDuckIME` and `TypeDuckRuntime` staging paths.
  </acceptance_criteria>
  <done>CI/release visible artifact surfaces are TypeDuck-owned and use `pwsh`.</done>
</task>

<task type="auto">
  <name>Task 3: Record final Phase 6 guard evidence</name>
  <read_first>
    .planning/phases/06-privacy-security-and-scaffold-cleanup/06-CONTEXT.md
    .planning/REQUIREMENTS.md
    scripts/Test-TypeDuckPrivacySecurityCleanup.ps1
    scripts/install.ps1
    scripts/_all_in_package.ps1
  </read_first>
  <files>.planning/product/privacy-security/phase-06-guard-results.json, scripts/Test-TypeDuckPrivacySecurityCleanup.ps1</files>
  <action>Have the aggregate guard write a deterministic JSON result record at `.planning/product/privacy-security/phase-06-guard-results.json` with UTC timestamp, repo root, backend root, guard commands run, pass/fail status, and a compact list of checked categories. The category list must include installer-cleanup with D-19 coverage for DLL registrations, startup entries, scheduled tasks, install files, exact scaffold residue filenames, and no broad user-state purge. Use invariant UTC formatting. Do not include raw typed content, environment secrets, or large file excerpts. Keep generated installer/stage outputs ignored; record guard evidence only.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckPrivacySecurityCleanup.ps1 -RepoRoot . -BackendRoot D:\VSProjects\moqi-ime -Strict</automated>
  </verify>
  <acceptance_criteria>
    `.planning/product/privacy-security/phase-06-guard-results.json` exists after the guard runs.
    The JSON contains `status: pass`, a UTC `generatedAt` ending in `Z`, and category names for identity, language, diagnostics, runtime, installer, installer-cleanup, and ipc.
    The JSON contains no raw typed sample text or secrets.
  </acceptance_criteria>
  <done>Phase 6 guard evidence is recorded without committing generated package binaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/resources -> guard suite | Static checks decide whether user-facing banned surfaces remain. |
| CI workflow -> release artifact | Workflow names and upload paths are visible release surfaces. |
| guard evidence -> planning docs | Evidence must not leak typed content or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | guard suite | mitigate | Aggregate focused guards and record deterministic JSON evidence. |
| T-06-06-02 | Spoofing | CI artifacts | mitigate | Rename workflow artifact and release upload paths to TypeDuck-owned values. |
| T-06-06-03 | Information Disclosure | guard evidence | mitigate | Store only category/status metadata, no raw typed content or secrets. |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo package install occurs in this plan. |
</threat_model>

<verification>
Run the aggregate Phase 6 guard plus existing focused guards it invokes. Do not run VM screenshot automation; Phase 7 owns broader install/host/release verification.
</verification>

<success_criteria>
All Phase 6 requirements are covered by strict static/scripted checks, visible release surfaces are TypeDuck-owned, and guard evidence is recorded.
</success_criteria>

<output>
Create `.planning/phases/06-privacy-security-and-scaffold-cleanup/06-06-SUMMARY.md` when done.
</output>
