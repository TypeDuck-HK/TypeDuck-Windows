---
phase: 07-compatibility-and-release-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - scripts/Invoke-TypeDuckReleaseVerification.ps1
  - scripts/Test-TypeDuckReleaseArtifacts.ps1
  - scripts/Test-TypeDuckReleaseVerification.ps1
  - .planning/product/release-fixtures/phase-07/protocol-recovery-results.json
  - .planning/product/release-fixtures/phase-07/release-verification.json
  - .planning/product/release-fixtures/phase-07/verification-notes.md
autonomous: true
requirements:
  - VER-03
  - VER-04
  - VER-05
  - VER-06
must_haves:
  truths:
    - "Developer can produce a TypeDuck release installer artifact with TypeDuck naming and SHA-256 evidence."
    - "Release verification evidence aggregates install, typing, host-app/DPI, protocol recovery, cleanup/security guard, artifact, and limitations records."
    - "If Phase 7 detects a product failure, the evidence records the failure and routes the smallest repair path rather than adding new capabilities in release verification."
    - "GitHub workflow evidence uses `TypeDuck-Windows`, `TypeDuck-Windows-backend`, and `TypeDuck-HK/schema` on `aap2-alpha`, runs schema through the Rime deployer to generate the runtime build folder, does not use `rime-frost`, and does not upload schema as a standalone artifact."
  artifacts:
    - path: "scripts/Invoke-TypeDuckReleaseVerification.ps1"
      provides: "Aggregate release evidence runner"
      contains: "release-verification.json"
    - path: "scripts/Test-TypeDuckReleaseArtifacts.ps1"
      provides: "Artifact naming, workflow path, and hash guard"
      contains: "typeduck-windows-ime-setup.exe"
    - path: "scripts/Test-TypeDuckReleaseVerification.ps1"
      provides: "Aggregate manifest validator"
      contains: "artifact-name-and-sha256"
    - path: ".planning/product/release-fixtures/phase-07/protocol-recovery-results.json"
      provides: "Executed protocol recovery result source consumed by aggregate validation"
    - path: ".planning/product/release-fixtures/phase-07/release-verification.json"
      provides: "Final release evidence manifest"
  key_links:
    - from: "scripts/Invoke-TypeDuckReleaseVerification.ps1"
      to: "installer/dist/typeduck-windows-ime-setup.exe"
      via: "build/package command and SHA-256 evidence"
      pattern: "Get-FileHash"
    - from: "scripts/Test-TypeDuckReleaseVerification.ps1"
      to: ".planning/product/release-fixtures/phase-07/release-verification.json"
      via: "required section and command evidence validation"
      pattern: "artifact-name-and-sha256"
    - from: "scripts/Test-TypeDuckReleaseVerification.ps1"
      to: ".planning/product/release-fixtures/phase-07/protocol-recovery-results.json"
      via: "validator requires executed recovery result entries before aggregate pass"
      pattern: "backend-timeout"
---

<objective>
Create the final aggregate release evidence and artifact verification plan.

Purpose: Phase 7's goal is achieved only when installer lifecycle, host-app/DPI judgement, protocol recovery, cleanup/security guard results, and TypeDuck release artifact facts converge into one repeatable manifest. This plan consumes Plans 07-01 through 07-03 and enforces D-14 through D-16: Phase 7 adds tests, scripts, docs, manifests, and packaging checks, while non-test product code changes are routed as focused repairs only when release-blocking failures are found.

Output: Release artifact guard, aggregate verification runner, aggregate manifest validator, and final evidence files under `.planning/product/release-fixtures/phase-07`.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-compatibility-and-release-verification/07-CONTEXT.md
@.planning/phases/07-compatibility-and-release-verification/07-PATTERNS.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-01-PLAN.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-02-PLAN.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-03-PLAN.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-04-PLAN.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-05-PLAN.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-06-PLAN.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-07-PLAN.md
@.planning/product/TYPEDUCK-IDENTITY-CONTRACT.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@scripts/_all_in_package.ps1
@scripts/install.ps1
@installer/build-installer.ps1
@.github/workflows/release.yml
@.github/workflows/nightly.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TypeDuck release artifact checks</name>
  <files>scripts/Test-TypeDuckReleaseArtifacts.ps1</files>
  <behavior>
    - Test 1: The guard fails when package scripts or workflows reference the old Moqi release asset path instead of `installer/dist/typeduck-windows-ime-setup.exe`.
    - Test 2: The guard fails when workflow artifact names are not TypeDuck-owned release/nightly names.
    - Test 3: The guard verifies a present installer artifact by name, byte length, and SHA-256 without force-adding ignored generated binaries.
    - Test 4: The guard fails when workflows use `moqi-im-windows`, `moqi-ime`, or `rime-frost` instead of `TypeDuck-Windows`, `TypeDuck-Windows-backend`, and `https://github.com/TypeDuck-HK/schema` on `aap2-alpha`.
    - Test 5: The guard fails when schema checkout/build outputs are uploaded as their own artifact instead of being consumed into the backend/runtime package and final installer evidence.
  </behavior>
  <action>Create `scripts/Test-TypeDuckReleaseArtifacts.ps1` to validate TypeDuck release artifact names, workflow repository names, schema source, and hash evidence per D-06, D-10, D-17 through D-20, and VER-06. It should inspect package scripts and CI workflow paths after Phase 6 has done product-surface cleanup, but it must not implement workflow/product corrections itself unless the executor explicitly enters a focused repair path for a release blocker per D-14 and D-15. The guard must require frontend checkout/path naming to use `TypeDuck-Windows`, backend checkout/path naming to use `TypeDuck-Windows-backend`, schema checkout to use `https://github.com/TypeDuck-HK/schema` with temporary branch `aap2-alpha`, and a Rime deployer step that generates the runtime `build` folder before packaging. It must reject `rime-frost` and reject workflow uploads that publish schema checkout/build output as a standalone artifact. The guard should accept generated installer binaries as ignored outputs and record hashes in evidence rather than committing binaries. Use `pwsh` command examples per project convention.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckReleaseArtifacts.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <done>The release artifact guard passes only when release/nightly workflow names, repo checkout names, backend/schema source names, package scripts, Rime deployer build-folder generation, and optional generated installer evidence are TypeDuck-owned and hash-recorded, with no standalone schema artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the aggregate release verification runner and validator</name>
  <files>scripts/Invoke-TypeDuckReleaseVerification.ps1, scripts/Test-TypeDuckReleaseVerification.ps1, .planning/product/release-fixtures/phase-07/protocol-recovery-results.json, .planning/product/release-fixtures/phase-07/release-verification.json</files>
  <behavior>
    - Test 1: The validator fails when required manifest sections are missing: installer, guard_commands, automated_checks, vm_evidence, interactive_human_checks, protocol_recovery, host_apps, dpi, release_artifacts, limitations, rerun_after_repairs.
    - Test 2: The validator fails when any required case ID from Plans 07-01 through 07-03 is absent, including artifact-name-and-sha256.
    - Test 3: The validator fails when protocol recovery cases are present only in `protocol-recovery-cases.json` and lack executed results in `protocol-recovery-results.json`.
    - Test 4: The validator fails when screenshot capture fields are required anywhere in the release manifest.
    - Test 5: The runner records command, status, UTC timestamp, and artifact path for each guard it invokes.
  </behavior>
  <action>Create `scripts/Invoke-TypeDuckReleaseVerification.ps1` as the aggregate runner that builds or validates the current installer artifact, invokes Phase 6 cleanup/security guards when present, invokes Plan 07-01 install evidence guard, invokes Plan 07-02 `scripts\Invoke-TypeDuckProtocolRecoveryProbe.ps1`, `ProtocolRecovery_test`, and `scripts\Test-TypeDuckProtocolRecovery.ps1`, invokes Plan 07-03 interactive checklist guard, and invokes `scripts\Test-TypeDuckReleaseArtifacts.ps1`, then writes `.planning/product/release-fixtures/phase-07/release-verification.json`. The aggregate manifest must embed or link `.planning/product/release-fixtures/phase-07/protocol-recovery-results.json` and copy the executed result status for backend-timeout, backend-restart-crash, settings-update-redeploy-failure, and bounded-degraded-state. Create `scripts/Test-TypeDuckReleaseVerification.ps1` to validate final manifest shape, required case IDs, and executed-result linkage. The validator must fail when those recovery cases are only documented in `protocol-recovery-cases.json`, when the result file is absent, when any required live recovery result has executed other than true, or when status is missing. The manifest must document `rerun_after_repairs` with the D-15 rule: if a release-blocking product bug is found, route the smallest repair path, rerun the affected verification, and then rerun the aggregate manifest. The runner and validator may create or update evidence docs/manifests per D-16, but must not add product capabilities per D-14.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckReleaseVerification.ps1 -RepoRoot . -Strict</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckProtocolRecoveryProbe.ps1 -RepoRoot . -BuildDir build-vs32 -EvidencePath .planning\product\release-fixtures\phase-07\protocol-recovery-results.json -Strict</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckReleaseVerification.ps1 -RepoRoot . -EvidenceRoot .planning\product\release-fixtures\phase-07</automated>
  </verify>
  <done>The aggregate runner writes `release-verification.json`, and the validator confirms all required release evidence sections/cases exist, live protocol recovery cases came from executed results, and no screenshot automation requirements are present.</done>
</task>

<task type="auto">
  <name>Task 3: Record final release notes and source audit</name>
  <files>.planning/product/release-fixtures/phase-07/verification-notes.md, .planning/product/release-fixtures/phase-07/release-verification.json</files>
  <action>Write the human-readable Phase 7 release verification notes summarizing installer artifact path/hash, install lifecycle evidence, protocol recovery evidence, interactive host-app/DPI note status, cleanup/security guard status, residual risks, and repair rerun status. Include a multi-source audit covering the ROADMAP goal, VER-03 through VER-06, and CONTEXT D-01 through D-16. Mark research as excluded because the user requested `--skip-research` and no RESEARCH.md was produced. The notes must state that D-01 through D-05 keep screenshot automation out of release evidence and that host-app/DPI judgement is recorded as human notes only.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckReleaseVerification.ps1 -RepoRoot . -Strict</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckReleaseArtifacts.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <done>`verification-notes.md` and `release-verification.json` provide a complete Phase 7 release evidence packet, including source audit and residual risks.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Developer can produce a TypeDuck installer artifact backed by repeatable Windows installation, typing, UI, protocol, and release evidence | 07-01, 07-02, 07-03, 07-04 | COVERED | Final aggregate plan consumes all evidence slices.
REQ | VER-03 | Clean install, upgrade/reinstall, uninstall, reboot-required registration | 07-01, 07-04 | COVERED | Install evidence and final manifest.
REQ | VER-04 | Typing and candidate UI in host apps, DPI, multi-monitor disposition | 07-03, 07-04 | COVERED | Interactive no-screenshot notes and aggregate manifest.
REQ | VER-05 | Protocol and engine behavior golden tests | 07-02, 07-04 | COVERED | Protocol recovery matrix/tests and aggregate manifest.
REQ | VER-06 | TypeDuck installer artifact and documented verification evidence | 07-01, 07-04 | COVERED | Artifact guard, hash evidence, final notes.
CONTEXT | D-01-D-05 | Screenshot boundary, DPI values, Phase 5 multi-monitor boundary | 07-01, 07-03, 07-04 | COVERED | Runner/validator reject screenshot automation.
CONTEXT | D-06-D-10 | Release evidence matrix and non-screenshot documentation | 07-01, 07-03, 07-04 | COVERED | Commands/logs/hashes/human notes feed final evidence.
CONTEXT | D-11-D-13 | Protocol/recovery cases and failure policy | 07-02, 07-04 | COVERED | Protocol matrix, executed recovery results, and aggregate evidence.
CONTEXT | D-14-D-16 | Product-change boundary and allowed Phase 7 artifacts | 07-04 | COVERED | Aggregate runner documents repair routing and avoids capability work.
CONTEXT | D-17-D-20 | Workflow repository names, schema source, Rime deployer build folder, and no schema artifact | 07-04 | COVERED | Release artifact guard validates workflow/source behavior.
RESEARCH | - | Research skipped by user request | - | EXCLUDED | No RESEARCH.md exists and `--skip-research` was requested.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-output-to-release | Generated installer output is promoted into release evidence. |
| prior-plan-evidence-to-aggregate | Evidence from install, protocol, and human checklist plans is summarized in one manifest. |
| CI-workflow-to-release-asset | GitHub workflows upload the named release/nightly artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | final installer artifact | mitigate | Require TypeDuck name, byte length, SHA-256, build command evidence, and generated-at UTC in the release manifest. |
| T-07-04-02 | Repudiation | aggregate manifest | mitigate | Runner records each guard command, exit status, timestamp, and artifact path. |
| T-07-04-03 | Information Disclosure | release evidence packet | mitigate | Validator rejects screenshot-dependent evidence and the notes avoid personal typed content. |
| T-07-04-04 | Spoofing | CI artifact naming | mitigate | Release artifact guard validates TypeDuck-owned workflow artifact names and installer upload path. |
| T-07-04-05 | Tampering | release-blocking repair path | mitigate | D-15 rerun policy is recorded: smallest focused repair, rerun affected evidence, rerun aggregate manifest. |
| T-07-04-06 | Repudiation | protocol recovery aggregation | mitigate | Validator fails unless live recovery cases have executed `protocol-recovery-results.json` entries linked from the aggregate manifest. |
| T-07-SC | Tampering | package-manager installs | accept | This plan adds no npm, pip, or cargo installs. |
</threat_model>

<verification>
Overall checks:
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckReleaseArtifacts.ps1 -RepoRoot . -Strict`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckProtocolRecoveryProbe.ps1 -RepoRoot . -BuildDir build-vs32 -EvidencePath .planning\product\release-fixtures\phase-07\protocol-recovery-results.json -Strict`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckReleaseVerification.ps1 -RepoRoot . -Strict`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckReleaseVerification.ps1 -RepoRoot . -EvidenceRoot .planning\product\release-fixtures\phase-07`
- Confirm `release-verification.json` includes install, protocol, host-app/DPI, artifact, limitations, and rerun-after-repairs sections with no screenshot automation requirement, and links or embeds executed `protocol-recovery-results.json` statuses.
</verification>

<success_criteria>
The final Phase 7 evidence packet proves VER-03 through VER-06 with TypeDuck artifact hash evidence, repeatable deterministic checks, human host-app/DPI notes, and clear release-blocking repair rerun rules.
</success_criteria>

<output>
Create `.planning/phases/07-compatibility-and-release-verification/07-04-SUMMARY.md` when done.
</output>
