---
phase: 07-compatibility-and-release-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/TypeduckBackendProbe/main.cpp
  - Tools/TypeduckBackendProbe/CMakeLists.txt
  - Tests/TypeDuckProtocol/ProtocolRecovery_test.cpp
  - Tests/TypeDuckProtocol/CMakeLists.txt
  - scripts/Invoke-TypeDuckProtocolRecoveryProbe.ps1
  - scripts/Test-TypeDuckProtocolRecovery.ps1
  - .planning/product/release-fixtures/phase-07/protocol-recovery-cases.json
  - .planning/product/release-fixtures/phase-07/protocol-recovery-results.json
autonomous: true
requirements:
  - VER-05
must_haves:
  truths:
    - "Developer can run repeatable protocol and recovery tests for normal input, lookup payloads, malformed frames, invalid protobuf, timeouts, restarts, stale responses, settings redeploy failure, and bounded degraded states."
    - "Technical recovery detail remains in logs/evidence and routine user-facing UI remains non-technical unless typing is unrecoverable or tampering is suspected."
  artifacts:
    - path: "Tests/TypeDuckProtocol/ProtocolRecovery_test.cpp"
      provides: "GoogleTest protocol recovery coverage"
      contains: "stale"
    - path: "Tools/TypeduckBackendProbe/main.cpp"
      provides: "Focused live protocol/backend recovery probe"
      contains: "protocol-recovery-results"
    - path: "scripts/Invoke-TypeDuckProtocolRecoveryProbe.ps1"
      provides: "Repeatable non-screenshot recovery probe runner"
      contains: "backend-timeout"
    - path: "scripts/Test-TypeDuckProtocolRecovery.ps1"
      provides: "Protocol recovery matrix and result validator"
      contains: "protocol-recovery-results.json"
    - path: ".planning/product/release-fixtures/phase-07/protocol-recovery-cases.json"
      provides: "Release protocol recovery case matrix"
    - path: ".planning/product/release-fixtures/phase-07/protocol-recovery-results.json"
      provides: "Executed recovery case evidence"
  key_links:
    - from: "Tests/TypeDuckProtocol/ProtocolRecovery_test.cpp"
      to: "proto/ProtoFraming.h"
      via: "FrameBuffer and parsePayload tests"
      pattern: "FrameBuffer"
    - from: "scripts/Invoke-TypeDuckProtocolRecoveryProbe.ps1"
      to: "Tools/TypeduckBackendProbe/main.cpp"
      via: "builds/runs focused recovery suite and writes JSON results"
      pattern: "protocol-recovery-results"
    - from: "scripts/Test-TypeDuckProtocolRecovery.ps1"
      to: ".planning/product/release-fixtures/phase-07/protocol-recovery-results.json"
      via: "requires executed result objects for live recovery cases"
      pattern: "executed"
---

<objective>
Add the repeatable protocol and recovery verification layer for release readiness.

Purpose: VER-05 requires golden tests for normal input, dictionary lookup, reverse lookup, malformed frames, timeouts, and backend restart. Phase 7 D-11 expands that list with invalid protobuf, oversized frames, stale/mismatched sequence responses, settings update/redeploy failure, and bounded degraded states. This plan keeps the tests non-visual and repeatedly runnable per D-12.

Output: A protocol recovery GoogleTest target, a focused backend recovery probe/runner, a release case matrix, and executed JSON recovery evidence that later aggregate evidence can consume.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-compatibility-and-release-verification/07-CONTEXT.md
@.planning/phases/07-compatibility-and-release-verification/07-PATTERNS.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-01-SUMMARY.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-02-SUMMARY.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-03-SUMMARY.md
@.planning/phases/04-typeduck-protocol-and-typing-mvp/04-04-SUMMARY.md
@.planning/phases/06-privacy-security-and-scaffold-cleanup/06-05-PLAN.md
@.planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md
@proto/ProtoFraming.h
@proto/moqi.proto
@Tests/TypeDuckProtocol/ProtoFraming_test.cpp
@Tests/TypeDuckProtocol/CMakeLists.txt
@MoqLauncher/PipeClient.cpp
@MoqLauncher/BackendServer.cpp
@MoqiTextService/MoqiClient.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the focused backend recovery probe and JSON results</name>
  <files>Tools/TypeduckBackendProbe/main.cpp, Tools/TypeduckBackendProbe/CMakeLists.txt, scripts/Invoke-TypeDuckProtocolRecoveryProbe.ps1, .planning/product/release-fixtures/phase-07/protocol-recovery-results.json</files>
  <behavior>
    - Test 1: The probe records an executed backend-timeout result with timeout duration, observed recovery status, recoverable flag, and no screenshot fields.
    - Test 2: The probe records an executed backend-restart-crash result after a deterministic mock backend exits or breaks stdout, then verifies the next request reaches a restarted backend instance.
    - Test 3: The probe records an executed settings-update-redeploy-failure result by sending a settings/deploy request to a controlled backend failure path and observing the bounded failure response.
    - Test 4: The probe records an executed bounded-degraded-state result proving recovery errors are bounded, typed, recoverable where expected, and do not include raw typed content.
  </behavior>
  <action>Extend `Tools/TypeduckBackendProbe` with a focused `protocol-recovery` mode and add `scripts/Invoke-TypeDuckProtocolRecoveryProbe.ps1` to build/run it and write `.planning/product/release-fixtures/phase-07/protocol-recovery-results.json` per D-11 and D-12. The recovery mode must use a deterministic controlled backend behavior, either embedded in the probe process or launched as a child by the runner, so it does not depend on screenshots, TSF host apps, user typing, secrets, or broad VM automation. It must actually execute and record results for backend-timeout, backend-restart-crash, settings-update-redeploy-failure, and bounded-degraded-state; do not satisfy these cases from matrix entries, source greps, or C++ unit tests alone. Each result object must include case_id, executed=true, status, command_or_mode, started_at_utc, completed_at_utc, observed_error_code or observed_failure_kind, observed_health or degraded_state, recoverable, restart_count when applicable, artifact/log path when produced, and a redacted flag proving raw typed content was not written to evidence. Use existing protobuf framing helpers and existing TypeDuck error/health enum names. Keep all changes in test/tool/script/evidence files; do not change product runtime behavior unless a release failure repair is explicitly opened per D-14 and D-15.</action>
  <verify>
    <automated>cmake --build build-vs32 --config Debug --target TypeduckBackendProbe -- /m:1</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckProtocolRecoveryProbe.ps1 -RepoRoot . -BuildDir build-vs32 -EvidencePath .planning\product\release-fixtures\phase-07\protocol-recovery-results.json -Strict</automated>
  </verify>
  <done>`protocol-recovery-results.json` contains executed passed/failed result records for backend-timeout, backend-restart-crash, settings-update-redeploy-failure, and bounded-degraded-state, produced by a repeatable command with no screenshot or raw typed content evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused protocol recovery unit tests</name>
  <files>Tests/TypeDuckProtocol/ProtocolRecovery_test.cpp, Tests/TypeDuckProtocol/CMakeLists.txt</files>
  <behavior>
    - Test 1: A valid Cantonese input request/response frame serializes and parses with matching sequence number and candidate payload fields.
    - Test 2: Raw lookup-filter separators survive a full `ServerResponse` candidate page round trip, not only a standalone `CandidateEntry`.
    - Test 3: Invalid protobuf payloads parse to false and leave cleared messages.
    - Test 4: Oversized and malformed frames set bounded error state and clear buffered bytes.
    - Test 5: Stale or mismatched sequence responses are identifiable by helper-level expectations used by launcher routing.
  </behavior>
  <action>Add `ProtocolRecovery_test.cpp` beside `ProtoFraming_test.cpp` and wire a `ProtocolRecovery_test` executable in `Tests/TypeDuckProtocol/CMakeLists.txt`. Use existing generated-protobuf include precedence and link settings from `ProtoFraming_test`. Cover D-11 cases that can be isolated in C++ without a live TSF registration: normal Cantonese frames, raw dictionary lookup payload preservation/parsing expectations, invalid protobuf, malformed and oversized frames, and stale/mismatched sequence response detection. Do not introduce a broad integration framework or external runtime dependency. Do not use these unit tests as the only evidence for backend-timeout, backend-restart-crash, settings-update-redeploy-failure, or bounded-degraded-state; those cases must come from Task 1's executed probe results.</action>
  <verify>
    <automated>cmake --build build-vs32 --config Debug --target ProtocolRecovery_test -- /m:1</automated>
    <automated>build-vs32\Tests\TypeDuckProtocol\Debug\ProtocolRecovery_test.exe</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolRecovery.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <done>`ProtocolRecovery_test` builds and passes, and the PowerShell guard confirms the in-process frame/protobuf/stale cases are covered without replacing the live probe evidence required for timeout, restart/crash, redeploy failure, and bounded degraded states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind the matrix and guard to executed recovery evidence</name>
  <files>scripts/Test-TypeDuckProtocolRecovery.ps1, .planning/product/release-fixtures/phase-07/protocol-recovery-cases.json, .planning/product/release-fixtures/phase-07/protocol-recovery-results.json</files>
  <behavior>
    - Test 1: The guard fails when any D-11 required case ID is absent from `protocol-recovery-cases.json`.
    - Test 2: The guard fails when backend-timeout, backend-restart-crash, settings-update-redeploy-failure, or bounded-degraded-state lacks an executed result in `protocol-recovery-results.json`.
    - Test 3: The guard fails when malformed/oversized/invalid-protobuf/stale cases are not tied to `ProtocolRecovery_test`, `Proto::FrameBuffer`, `parsePayload`, or launcher degraded response evidence.
    - Test 4: The guard fails when the failure policy allows routine user-facing technical detail contrary to D-13 or when evidence contains raw typed content fields.
  </behavior>
  <action>Create or revise the Phase 7 protocol recovery matrix and guard so the matrix is an index, not the evidence. Required case IDs are: normal-cantonese-frames, raw-lookup-payload, reverse-lookup-if-supported, malformed-frame, oversized-frame, invalid-protobuf, backend-timeout, backend-restart-crash, stale-mismatched-sequence, settings-update-redeploy-failure, bounded-degraded-state. For backend-timeout, backend-restart-crash, settings-update-redeploy-failure, and bounded-degraded-state, require matching `protocol-recovery-results.json` entries with executed=true and a concrete status. For normal/protobuf/frame/stale cases, require `ProtocolRecovery_test` command evidence. Keep reverse lookup marked conditional on backend support rather than fabricating unsupported output, but still require the conditional result to state supported, unsupported, or skipped-with-reason. The guard must reject screenshot fields and raw typed content in evidence per D-01, D-02, D-10, and D-13.</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckProtocolRecoveryProbe.ps1 -RepoRoot . -BuildDir build-vs32 -EvidencePath .planning\product\release-fixtures\phase-07\protocol-recovery-results.json -Strict</automated>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolRecovery.ps1 -RepoRoot . -Strict</automated>
  </verify>
  <done>The guard passes only when the full D-11 case list is present and the live recovery cases have executed result records, not documentation-only or matrix-only coverage.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Repeatable protocol and release evidence | 07-02, 07-04 | COVERED | Protocol recovery matrix and tests feed final release evidence.
REQ | VER-05 | Protocol and engine behavior golden tests | 07-02 | COVERED | Required normal, lookup, reverse lookup, malformed, timeout, restart cases are planned with unit tests plus executed probe results.
CONTEXT | D-11 | Full protocol/recovery scenario list | 07-02 | COVERED | Matrix includes all scenarios named in D-11 and live recovery cases require result records.
CONTEXT | D-12 | Automate repeatable non-visual protocol checks | 07-02 | COVERED | Unit tests, focused probe, runner, and guards cover repeatedly testable cases.
CONTEXT | D-13 | Preserve Phase 6 failure policy | 07-02 | COVERED | Guard asserts technical detail is in logs/evidence, not routine UI.
RESEARCH | - | Research skipped by user request | - | EXCLUDED | No RESEARCH.md exists and `--skip-research` was requested.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-frame-to-launcher | Untrusted or broken TSF client frames enter launcher parsing. |
| backend-frame-to-launcher | Backend stdout can emit malformed, oversized, stale, or invalid protobuf frames. |
| launcher-to-user-facing-state | Recovery responses can affect composition/candidate UI and logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Denial of Service | `Proto::FrameBuffer` | mitigate | Unit tests verify bounded oversized/malformed frame rejection and buffer cleanup. |
| T-07-02-02 | Tampering | backend response sequencing | mitigate | Unit tests and guard require stale/mismatched sequence evidence before release. |
| T-07-02-03 | Information Disclosure | recovery UI | mitigate | Guard enforces D-13: technical detail goes to logs/evidence unless unrecoverable/tampering conditions apply. |
| T-07-02-04 | Repudiation | protocol recovery evidence | mitigate | Case matrix records case IDs, automation status, command, expected outcome, and artifact link. |
| T-07-02-05 | Denial of Service | backend timeout/restart path | mitigate | Focused recovery probe executes timeout and restart/crash cases and records bounded health/recovery evidence. |
| T-07-SC | Tampering | package-manager installs | accept | This plan adds no npm, pip, or cargo installs. |
</threat_model>

<verification>
Overall checks:
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolRecovery.ps1 -RepoRoot . -Strict`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-TypeDuckProtocolRecoveryProbe.ps1 -RepoRoot . -BuildDir build-vs32 -EvidencePath .planning\product\release-fixtures\phase-07\protocol-recovery-results.json -Strict`
- `cmake --build build-vs32 --config Debug --target ProtocolRecovery_test -- /m:1`
- `cmake --build build-vs32 --config Debug --target TypeduckBackendProbe -- /m:1`
- `build-vs32\Tests\TypeDuckProtocol\Debug\ProtocolRecovery_test.exe`
- Existing protocol guard remains green: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckProtocolContract.ps1 -RepoRoot . -Strict`
</verification>

<success_criteria>
The Phase 7 protocol/recovery matrix covers every D-11 case, runnable unit tests prove the deterministic frame/protobuf/payload/sequence cases, focused probe results prove timeout/restart/redeploy/degraded recovery cases, and the Phase 6 failure policy is explicitly enforced.
</success_criteria>

<output>
Create `.planning/phases/07-compatibility-and-release-verification/07-02-SUMMARY.md` when done.
</output>
