---
plan_id: "02-03"
phase: "02-engine-runtime-contract-spike"
phase_number: "02"
phase_name: "engine-runtime-contract-spike"
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "scripts/Test-TypeDuckLookupPayload.ps1"
  - ".planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md"
  - ".planning/product/TYPEDUCK-ENGINE-RUNTIME-CONTRACT.md"
  - ".planning/product/engine-runtime-fixtures/phase-02/lookup-filter-raw-comments.ndjson"
  - ".planning/product/engine-runtime-fixtures/phase-02/lookup-filter-csv-rows.csv"
  - ".planning/product/engine-runtime-fixtures/phase-02/lookup-filter-validation.json"
autonomous: true
requirements: ["ENG-05"]
requirements_addressed: ["ENG-05"]
must_haves:
  truths:
    - "D-09 D-10 D-11 D-12 D-13 ENG-05: Developer can capture raw lookup-filter candidate comment output and prove it contains dictionary-backed CSV/comment data for representative TypeDuck inputs."
    - "D-10: The raw CSV/comment evidence preserves the exact expected column order: match_input_buffer,honzi,jyutping,canonical_honzi,canonical_jyutping,components_honzi,components_jyutping,pron_label,lit_col_reading,pos,register,label,written_form,vernacular_form,collocation,eng,hin,urd,nep,ind."
    - "D-11 D-12: Evidence preserves optional leading \\v, note separator \\f, dictionary-entry separator \\r, quoted CSV fields, doubled quotes, and empty fields without stripping or normalizing control separators."
    - "D-16: The plan records raw evidence only; final protocol mapping and native dictionary UI remain outside Phase 2."
  artifacts:
    - path: "scripts/Test-TypeDuckLookupPayload.ps1"
      provides: "Raw lookup-filter payload validator for control separators and CSV columns"
      min_lines: 120
    - path: ".planning/product/engine-runtime-fixtures/phase-02/lookup-filter-raw-comments.ndjson"
      provides: "Raw candidate comment evidence with escaped and byte-level separator metadata"
    - path: ".planning/product/engine-runtime-fixtures/phase-02/lookup-filter-csv-rows.csv"
      provides: "Extracted dictionary rows with the exact D-10 header order"
    - path: ".planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md"
      provides: "Human-readable raw payload contract for Phase 4 and Phase 5 consumers"
  key_links:
    - from: "scripts/Test-TypeDuckLookupPayload.ps1"
      to: ".planning/product/engine-runtime-fixtures/phase-02/typing-proof.json"
      via: "reads raw candidate comments captured by Plan 02-02"
      pattern: "typing-proof"
    - from: ".planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md"
      to: ".planning/product/TYPEDUCK-WEB-ALPHA-FIXTURES-2026-06-23.md"
      via: "maps raw fields to the Web alpha dictionary evidence without creating the final UI model"
      pattern: "WEB-ALPHA-FIXTURES"
    - from: ".planning/product/TYPEDUCK-ENGINE-RUNTIME-CONTRACT.md"
      to: ".planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md"
      via: "runtime contract links the raw lookup-filter evidence"
      pattern: "LOOKUP-FILTER-RAW"
---

<objective>
Validate and document the raw dictionary lookup-filter payload contract.

Purpose: Prove that Phase 2 engine output includes the raw candidate comment payloads TypeDuck Windows needs for later protocol and candidate/dictionary UI work, while preserving exact control-character and CSV semantics from the Web alpha-compatible engine path.
Output: Raw comment evidence, extracted CSV row evidence, a validator script, validation JSON, and a raw payload contract.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engine-runtime-contract-spike/02-CONTEXT.md
@.planning/phases/01-identity-and-web-parity-contract/01-CONTEXT.md
@.planning/product/TYPEDUCK-WEB-ALPHA-FIXTURES-2026-06-23.md
@.planning/product/TYPEDUCK-BANNED-SURFACES.md
@.planning/product/TYPEDUCK-ENGINE-RUNTIME-CONTRACT.md
@.planning/product/engine-runtime-fixtures/phase-02/typing-proof.json
@proto/moqi.proto
@.planning/phases/02-engine-runtime-contract-spike/02-02-SUMMARY.md
</context>

## Phase Goal

**As a** TypeDuck Windows developer, **I want to** preserve and validate raw lookup-filter comment data from real engine output, **so that** later protocol and UI phases can map dictionary fields without guessing or losing separator semantics.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add raw lookup payload validation tests</name>
  <files>scripts/Test-TypeDuckLookupPayload.ps1</files>
  <behavior>
    - Test 1: The validator fails if lookup-filter-csv-rows.csv does not use the exact D-10 header order.
    - Test 2: The validator fails if raw comments lose optional leading reverse lookup marker \\v, note separator \\f, dictionary row prefix/separator \\r, or Jyutping-only \\f semantics.
    - Test 3: The validator accepts quoted CSV fields, doubled quotes inside quoted values, and empty fields without changing the raw comment payload.
  </behavior>
  <action>Create scripts/Test-TypeDuckLookupPayload.ps1 to read typing-proof.json and lookup-filter evidence. The script must validate the exact D-10 columns, parse CSV rows using standard comma separation with double-quoted fields and doubled quotes inside quoted values per D-12, and verify that raw comments preserve the D-11 control-separator contract. It must emit lookup-filter-validation.json with pass/fail details. Do not convert the payload into final TypeDuck protocol fields or UI model fields per D-09, D-13, and D-16.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLookupPayload.ps1 -TypingProofPath .planning\product\engine-runtime-fixtures\phase-02\typing-proof.json -RawCommentsPath .planning\product\engine-runtime-fixtures\phase-02\lookup-filter-raw-comments.ndjson -CsvRowsPath .planning\product\engine-runtime-fixtures\phase-02\lookup-filter-csv-rows.csv -ValidationPath .planning\product\engine-runtime-fixtures\phase-02\lookup-filter-validation.json; if ($LASTEXITCODE -eq 0) { throw 'Expected RED failure before lookup evidence exists' }</automated>
  </verify>
  <done>The validator exists, fails without lookup evidence, and enforces D-10, D-11, and D-12 without producing transformed product data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture raw lookup-filter comment and CSV evidence</name>
  <files>.planning/product/engine-runtime-fixtures/phase-02/lookup-filter-raw-comments.ndjson, .planning/product/engine-runtime-fixtures/phase-02/lookup-filter-csv-rows.csv, .planning/product/engine-runtime-fixtures/phase-02/lookup-filter-validation.json</files>
  <behavior>
    - Test 1: Raw comments from representative inputs include dictionary-backed candidate entries with headword, Jyutping, part-of-speech, English meaning, reading notes, and enabled-language meanings when available.
    - Test 2: lookup-filter-csv-rows.csv starts with the exact D-10 header and contains extracted rows from the raw comments.
    - Test 3: lookup-filter-validation.json records separator preservation and CSV parsing results.
  </behavior>
  <action>Use the Plan 02-02 proof output to extract raw candidate comment payloads into lookup-filter-raw-comments.ndjson and dictionary rows into lookup-filter-csv-rows.csv. Preserve raw text, escaped notation, and byte/character metadata for control separators; do not strip, replace, or normalize \\v, \\f, or \\r. Include representative TypeDuck inputs anchored to Web alpha fixtures, including ordinary Cantonese input and reverse lookup where available. Run scripts/Test-TypeDuckLookupPayload.ps1 until it passes and writes lookup-filter-validation.json. If the adapter proof lacks dictionary-backed comments, rerun Plan 02-02 proof with inputs that produce dictionary detail in the Web alpha fixture set, such as nei and housam, rather than fabricating rows.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Test-TypeDuckLookupPayload.ps1 -TypingProofPath .planning\product\engine-runtime-fixtures\phase-02\typing-proof.json -RawCommentsPath .planning\product\engine-runtime-fixtures\phase-02\lookup-filter-raw-comments.ndjson -CsvRowsPath .planning\product\engine-runtime-fixtures\phase-02\lookup-filter-csv-rows.csv -ValidationPath .planning\product\engine-runtime-fixtures\phase-02\lookup-filter-validation.json</automated>
  </verify>
  <done>Raw comment NDJSON, extracted CSV rows, and validation JSON exist and prove D-10 through D-12 for representative TypeDuck candidate dictionary data.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the raw lookup-filter contract and phase source audit</name>
  <files>.planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md, .planning/product/TYPEDUCK-ENGINE-RUNTIME-CONTRACT.md</files>
  <action>Create .planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md and update the engine runtime contract to link it. The raw contract must cite D-09 through D-13 and ENG-05, list the exact D-10 header, explain D-11 separator parsing, explain D-12 CSV parsing, point to lookup-filter-raw-comments.ndjson, lookup-filter-csv-rows.csv, and lookup-filter-validation.json, and explicitly state that Phase 4/5 will map raw columns to structured protocol/UI fields. Include the phase-wide source audit table showing GOAL, ENG-01 through ENG-05, and D-01 through D-16 as COVERED by plans 02-01, 02-02, and 02-03. Exclude deferred ideas from the audit and do not add schema/database push tasks.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/product/TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT.md'; $t=Get-Content -Raw -LiteralPath $p; @('D-09','D-10','D-11','D-12','D-13','ENG-05','match_input_buffer,honzi,jyutping,canonical_honzi,canonical_jyutping,components_honzi,components_jyutping,pron_label,lit_col_reading,pos,register,label,written_form,vernacular_form,collocation,eng,hin,urd,nep,ind','lookup-filter-raw-comments.ndjson','lookup-filter-csv-rows.csv','Source Audit') | ForEach-Object { if ($t -notmatch [regex]::Escape($_)) { throw \"Missing $_\" } }; $engine=Get-Content -Raw -LiteralPath '.planning/product/TYPEDUCK-ENGINE-RUNTIME-CONTRACT.md'; if ($engine -notmatch 'TYPEDUCK-LOOKUP-FILTER-RAW-CONTRACT') { throw 'engine contract missing raw lookup link' }"</automated>
  </verify>
  <done>The raw lookup-filter contract exists, the engine runtime contract links it, and the phase-wide source audit shows no unplanned GOAL/REQ/CONTEXT items.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 2 | Prove TypeDuck-HK librime, lookup-filter, schemas, dictionaries, and built assets produce candidate and dictionary data | 02-01, 02-02, 02-03 | COVERED | Runtime staging, adapter behavior proof, and raw lookup evidence together satisfy the ROADMAP goal.
REQ | ENG-01 | Package pinned TypeDuck-HK librime fork as engine dependency | 02-01, 02-02 | COVERED | D-06 release archive staged and then exercised.
REQ | ENG-02 | Package rime-dictionary-lookup-filter plugin with engine | 02-01, 02-03 | COVERED | Plugin evidence staged and lookup-filter payload validated.
REQ | ENG-03 | Include TypeDuck schemas, dictionaries, OpenCC assets, and built schema assets | 02-01 | COVERED | D-08 submodule and staged manifest prove assets.
REQ | ENG-04 | Produce Cantonese composition, candidate pages, Jyutping, and commit output | 02-02 | COVERED | Adapter proof captures typing-proof.json.
REQ | ENG-05 | Produce lookup-filter-backed dictionary data for candidates | 02-03 | COVERED | Raw comments, CSV rows, and validation JSON prove dictionary data.
CONTEXT | D-01 | Use current moqi-ime backend shape as internal compatibility adapter | 02-02 | COVERED | Probe and runner use adapter process boundary only.
CONTEXT | D-02 | Minimal changes to load TypeDuck-HK rime.dll and schema assets | 02-01, 02-02 | COVERED | Staging overlays existing Rime DLL loading path.
CONTEXT | D-03 | Focus code work on rime_api compatibility issues | 02-02 | COVERED | Adapter patch area records only required deltas.
CONTEXT | D-04 | Do not rewrite TSF frontend around direct in-process librime calls | 02-02 | COVERED | Direct probing only allowed as debug evidence after concrete adapter blockers.
CONTEXT | D-05 | Produce Windows-shaped staged TypeDuck runtime tree | 02-01 | COVERED | Stage-TypeDuckRuntime.ps1 and manifest.
CONTEXT | D-06 | Use pinned TypeDuck-HK librime release URL | 02-01 | COVERED | Exact URL is required by staging test.
CONTEXT | D-07 | Do not document how replacement rime.dll was produced | 02-01 | COVERED | Runtime contract treats release artifact as source.
CONTEXT | D-08 | Add TypeDuck-HK/schema branch aap2-alpha as submodule | 02-01 | COVERED | Submodule path third_party/typeduck-schema.
CONTEXT | D-09 | Prove raw lookup-filter CSV/comment output first | 02-03 | COVERED | Raw evidence contract only.
CONTEXT | D-10 | Preserve exact CSV/comment columns | 02-03 | COVERED | Validator enforces header.
CONTEXT | D-11 | Preserve control-character separator semantics | 02-03 | COVERED | Validator enforces \v, \f, \r behavior.
CONTEXT | D-12 | Standard CSV parsing with quoted fields and no separator normalization | 02-03 | COVERED | Validator enforces parsing and raw preservation.
CONTEXT | D-13 | Later Phase 4/5 maps raw columns into protocol/UI | 02-03 | COVERED | Contract says raw only.
CONTEXT | D-14 | Bridge does not permit shipping unchanged Moqi surfaces | 02-01, 02-02 | COVERED | Runtime and adapter contracts reference banned surfaces.
CONTEXT | D-15 | Legacy fallback behavior is not valid product behavior | 02-02 | COVERED | Proof rejects legacy-only evidence.
CONTEXT | D-16 | Broad protocol/product naming/installer/UI cleanup remains outside Phase 2 | 02-01, 02-02, 02-03 | COVERED | Each plan includes boundary language.
RESEARCH | skipped | User invoked --skip-research; no 02-RESEARCH.md source exists | NONE | EXCLUDED | Explicitly skipped by user; not a missing item.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine proof output -> raw lookup evidence | Backend output is parsed into planning evidence without changing the raw payload semantics. |
| Raw comments -> CSV validation | Control-character payloads and CSV rows cross from engine output into developer validation scripts. |
| Planning evidence -> later protocol/UI phases | Raw evidence becomes source material for future structured mapping without being the mapping itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | lookup-filter-raw-comments.ndjson | mitigate | Preserve raw payload, escaped notation, and separator metadata; validation fails if separators are lost. |
| T-02-03-02 | Tampering | lookup-filter-csv-rows.csv | mitigate | Enforce the exact D-10 header and standard quoted CSV parsing. |
| T-02-03-03 | Repudiation | lookup-filter-validation.json | mitigate | Record input files, validation cases, pass/fail status, and timestamp. |
| T-02-03-04 | Information Disclosure | captured lookup evidence | mitigate | Use controlled fixture inputs only and avoid personal typed content. |
| T-02-03-05 | Elevation of Privilege | later consumers misusing raw payload | accept | Phase 2 does not execute payload contents; later protocol/UI phases must treat raw strings as untrusted display data. |
</threat_model>

<verification>
Run the task-level commands in order. Then run:
powershell -NoProfile -Command "$csv='.planning/product/engine-runtime-fixtures/phase-02/lookup-filter-csv-rows.csv'; $header=(Get-Content -LiteralPath $csv -First 1); if ($header -ne 'match_input_buffer,honzi,jyutping,canonical_honzi,canonical_jyutping,components_honzi,components_jyutping,pron_label,lit_col_reading,pos,register,label,written_form,vernacular_form,collocation,eng,hin,urd,nep,ind') { throw 'bad lookup CSV header' }"
</verification>

<success_criteria>
ENG-05 is satisfied for the spike when raw lookup-filter comments and extracted CSV rows prove dictionary-backed candidate data with the exact D-10 columns and D-11/D-12 separator/CSV semantics preserved.
</success_criteria>

<deviation_handling>
If raw comments contain only Jyutping-only pronunciation data after the first \f and no \r dictionary rows, record that input as non-dictionary evidence and rerun the proof with fixture-backed inputs expected to have dictionary details. If no representative input emits dictionary rows through the TypeDuck runtime, mark ENG-05 blocked with captured raw output rather than normalizing comments or inventing dictionary data.
</deviation_handling>

<output>
Create `.planning/phases/02-engine-runtime-contract-spike/02-03-SUMMARY.md` when done.
</output>
